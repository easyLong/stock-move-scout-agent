# Auto-generated by refactor: static web templates for stock_scout_web.py.
from __future__ import annotations

HTML = r"""<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>异动情报引擎</title>
  <style>
    :root {
      color-scheme: light;
      --bg: #f5f7fb;
      --panel: #ffffff;
      --soft: #f8fafc;
      --line: #e4e8f0;
      --line-strong: #cbd5e1;
      --text: #172033;
      --muted: #667085;
      --accent: #2563eb;
      --scan: #596579;
      --window: #b4553a;
      --auction: #a15c22;
      --anchor-text: #08736b;
      --anchor-bg: #edfdfa;
      --anchor-line: #a7f3e8;
      --highlight-text: #635b8f;
      --highlight-bg: #f3f0ff;
      --leader-text: #a33a3a;
      --leader-bg: #fff1f2;
      --leader-line: #fecdd3;
      --core-text: #315a9f;
      --core-bg: #eff6ff;
      --core-line: #bfdbfe;
      --metric-change-text: #9a4a1f;
      --metric-change-bg: #fff7ed;
      --metric-change-line: #fed7aa;
      --metric-speed-text: #2457a6;
      --metric-speed-bg: #eef6ff;
      --metric-speed-line: #bfdbfe;
      --shadow: 0 12px 30px rgba(30, 41, 59, 0.06);
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      background: var(--bg);
      color: var(--text);
      font-family: "Microsoft YaHei", "PingFang SC", system-ui, -apple-system, Segoe UI, sans-serif;
      font-size: 14px;
      line-height: 1.5;
    }
    .topbar {
      position: sticky;
      top: 0;
      z-index: 10;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 16px;
      min-height: 56px;
      padding: 0 22px;
      background: rgba(255, 255, 255, 0.94);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid var(--line);
    }
    h1 {
      margin: 0;
      font-size: 17px;
      font-weight: 700;
      letter-spacing: 0;
      white-space: nowrap;
    }
    .nav-link {
      color: #334155;
      text-decoration: none;
      border: 1px solid var(--line);
      border-radius: 7px;
      padding: 4px 9px;
      background: #fff;
      font-size: 12px;
      font-weight: 650;
      white-space: nowrap;
    }
    .nav-link:hover {
      color: var(--accent);
      border-color: #bfdbfe;
    }
    .meta {
      display: flex;
      align-items: center;
      gap: 12px;
      color: var(--muted);
      font-size: 12px;
      min-width: 0;
    }
    .dot {
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: #2e9f7b;
      display: inline-block;
    }
    main {
      width: min(1680px, calc(100vw - 16px));
      margin: 0 auto;
      padding: 14px 0 34px;
    }
    .summarybar {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      margin-bottom: 10px;
      color: var(--muted);
      font-size: 12px;
    }
    .summarybar span {
      background: #fff;
      border: 1px solid var(--line);
      border-radius: 999px;
      padding: 4px 9px;
    }
    .toolbar {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 8px;
      flex-wrap: wrap;
    }
    .date-picker {
      height: 32px;
      min-width: 142px;
      border: 1px solid var(--line);
      border-radius: 7px;
      background: #fff;
      color: #111827;
      padding: 0 8px;
      font-size: 13px;
    }
    .toolbtn {
      height: 32px;
      border: 1px solid var(--line);
      border-radius: 7px;
      background: #fff;
      color: #334155;
      padding: 0 10px;
      font-size: 13px;
      cursor: pointer;
    }
    .toolbtn:hover {
      border-color: #bfdbfe;
      color: var(--accent);
    }
    .intel-layout {
      display: grid;
      grid-template-columns: minmax(0, 1fr) 360px;
      gap: 12px;
      align-items: start;
    }
    .detail-panel, .feed-panel {
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: 10px;
      overflow: hidden;
      box-shadow: var(--shadow);
    }
    .detail-title, .feed-title {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      min-height: 38px;
      padding: 0 12px;
      border-bottom: 1px solid var(--line);
      color: #1f2937;
      font-size: 13px;
      font-weight: 700;
      background: linear-gradient(#ffffff, #fafbfc);
    }
    .detail-title small, .feed-title small {
      color: var(--muted);
      font-weight: 400;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .feed-title small {
      white-space: nowrap;
    }
    .metric-chip {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      height: 22px;
      min-width: 66px;
      padding: 0 7px;
      border-radius: 999px;
      background: #f8fafc;
      border: 1px solid #e2e8f0;
      color: #566174;
      font-size: 12px;
      font-weight: 600;
      font-variant-numeric: tabular-nums;
      white-space: nowrap;
    }
    .metric-chip.change {
      color: var(--metric-change-text);
      background: var(--metric-change-bg);
      border-color: var(--metric-change-line);
    }
    .metric-chip.speed {
      color: var(--metric-speed-text);
      background: var(--metric-speed-bg);
      border-color: var(--metric-speed-line);
    }
    .detail-panel {
      position: sticky;
      top: 70px;
      max-height: calc(100vh - 84px);
    }
    #detailPanel {
      overflow: visible;
    }
    .detail-body {
      max-height: calc(100vh - 125px);
      padding: 12px 14px 14px;
      overflow-y: auto;
      overflow-x: hidden;
      overscroll-behavior: contain;
      scrollbar-gutter: stable;
    }
    .detail-empty {
      padding: 16px 12px;
      color: var(--muted);
    }
    .detail-shell {
      display: grid;
      gap: 10px;
    }
    .detail-hero {
      border: 1px solid #dbe3ea;
      border-radius: 10px;
      background: linear-gradient(180deg, #ffffff 0%, #fbfdff 100%);
      padding: 12px 13px;
      display: grid;
      gap: 8px;
      box-shadow: 0 1px 0 rgba(15, 23, 42, 0.03);
    }
    .detail-hero-top {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 10px;
      min-width: 0;
    }
    .detail-hero-title {
      min-width: 0;
      display: grid;
      gap: 3px;
    }
    .detail-stock {
      color: #0f172a;
      font-size: 15px;
      font-weight: 850;
      line-height: 1.2;
    }
    .detail-meta {
      color: #64748b;
      font-size: 12px;
      font-weight: 650;
      line-height: 1.35;
    }
    .detail-level {
      flex: 0 0 auto;
      border-radius: 999px;
      padding: 3px 9px;
      border: 1px solid #d9dce7;
      background: #f7f9fc;
      color: #334155;
      font-size: 12px;
      font-weight: 800;
      white-space: nowrap;
    }
    .detail-level.strong {
      border-color: #b7ded3;
      background: #edf9f5;
      color: #23654b;
    }
    .detail-level.warn {
      border-color: #f0d6a7;
      background: #fff8ea;
      color: #7a5522;
    }
    .detail-reason {
      color: #111827;
      font-size: 13px;
      line-height: 1.65;
      font-weight: 650;
    }
    .detail-chip-row {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
    }
    .detail-card-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 8px;
    }
    .detail-card {
      min-width: 0;
      border: 1px solid #e5e7eb;
      border-radius: 10px;
      background: #fff;
      padding: 10px 11px;
      display: grid;
      gap: 7px;
      box-shadow: 0 1px 0 rgba(15, 23, 42, 0.03);
    }
    .detail-card.theme {
      border-left: 3px solid var(--anchor-line);
    }
    .detail-card.stock {
      border-left: 3px solid var(--core-line);
    }
    .detail-card.event, .detail-card.announcement {
      border-left: 3px solid var(--metric-change-line);
    }
    .detail-card.impact {
      border-left: 3px solid #f0b86a;
      background: #fffaf2;
    }
    .detail-card.summary {
      border-left: 3px solid #c4b5fd;
      background: #fbfaff;
    }
    .detail-card-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      min-width: 0;
    }
    .detail-card-title {
      min-width: 0;
      color: #0f172a;
      font-size: 13px;
      font-weight: 850;
      line-height: 1.3;
    }
    .detail-card-source {
      flex: 0 0 auto;
      color: #64748b;
      font-size: 11px;
      font-weight: 650;
      white-space: nowrap;
    }
    .detail-card-body {
      color: #374151;
      font-size: 12px;
      line-height: 1.6;
      display: grid;
      gap: 6px;
    }
    .detail-card-body .impact-item {
      padding: 7px 8px;
    }
    .detail-card-body .theme-role-grid {
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    }
    .detail-card-body .theme-role-card {
      gap: 5px;
      padding: 8px 9px;
    }
    .detail-card-body .theme-role-reason {
      -webkit-line-clamp: 1;
    }
    .evidence-summary {
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      background: #fff;
      padding: 12px;
      margin-bottom: 12px;
      color: #374151;
      font-size: 13px;
      line-height: 1.5;
    }
    .evidence-verdict {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      padding-bottom: 8px;
      margin-bottom: 8px;
      border-bottom: 1px solid #eef2f7;
    }
    .evidence-verdict strong {
      color: #111827;
      font-size: 14px;
    }
    .evidence-level {
      border-radius: 999px;
      padding: 1px 8px;
      font-size: 12px;
      font-weight: 700;
      background: #eff6ff;
      color: #2457a6;
      border: 1px solid #bfdbfe;
      white-space: nowrap;
    }
    .evidence-level.strong {
      background: #ecfdf5;
      color: #047857;
      border-color: #a7f3d0;
    }
    .evidence-level.weak {
      background: #fafafa;
      color: #71717a;
      border-color: #e4e4e7;
    }
    .evidence-basis {
      color: #536174;
      margin-top: 8px;
    }
    .evidence-focus-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 6px;
      margin: 9px 0 10px;
    }
    .evidence-focus {
      min-width: 0;
      display: grid;
      grid-template-columns: 82px minmax(0, 1fr);
      column-gap: 10px;
      border: 1px solid #e5e7eb;
      border-left: 3px solid #d8dee9;
      border-radius: 8px;
      background: #fbfcfe;
      padding: 8px 9px;
    }
    .evidence-focus.good {
      border-left-color: #4f9d7e;
    }
    .evidence-focus.hot {
      border-left-color: #cf6f62;
    }
    .evidence-focus.rank {
      border-left-color: #7c8cc7;
    }
    .evidence-focus-title {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      justify-content: flex-start;
      gap: 3px;
      color: #1f2937;
      font-size: 12px;
      font-weight: 800;
      padding-top: 1px;
    }
    .evidence-focus-title small {
      color: var(--muted);
      font-size: 11px;
      font-weight: 650;
      line-height: 1.35;
    }
    .evidence-focus-lines {
      display: grid;
      gap: 4px;
      color: #374151;
      font-size: 12px;
      line-height: 1.5;
    }
    .evidence-focus-line {
      display: -webkit-box;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 2;
      overflow: hidden;
    }
    .evidence-flow {
      display: flex;
      gap: 6px;
      flex-wrap: wrap;
      margin: 8px 0 0;
    }
    .evidence-flow-chip {
      border: 1px solid #e2e8f0;
      background: #f8fafc;
      color: #475569;
      border-radius: 999px;
      padding: 2px 8px;
      font-size: 11px;
      font-weight: 700;
      white-space: nowrap;
    }
    .decision-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 6px;
      margin: 9px 0 7px;
    }
    .decision-card {
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      background: #f9fafb;
      padding: 7px 8px;
      min-width: 0;
    }
    .decision-label {
      display: block;
      color: #64748b;
      font-size: 11px;
      font-weight: 700;
      margin-bottom: 2px;
    }
    .decision-value {
      display: block;
      color: #111827;
      font-size: 13px;
      font-weight: 750;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .evidence-chip-row {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
      margin-top: 6px;
    }
    .evidence-chip {
      display: inline-flex;
      align-items: center;
      max-width: 100%;
      border-radius: 999px;
      border: 1px solid #e2e8f0;
      background: #fff;
      color: #475569;
      padding: 2px 8px;
      font-size: 12px;
      font-weight: 650;
      line-height: 1.5;
    }
    .evidence-chip.good {
      border-color: #badbcc;
      background: #f1fbf6;
      color: #23654b;
    }
    .evidence-chip.warn {
      border-color: #f0d6a7;
      background: #fff8ea;
      color: #7a5522;
    }
    .evidence-chip.weak {
      border-color: #e1e5eb;
      background: #f8fafc;
      color: #64748b;
    }
    .evidence-key {
      color: #1f2937;
      font-weight: 650;
      margin-top: 2px;
    }
    .evidence-facts {
      display: grid;
      gap: 5px;
      margin: 2px 0 0;
      padding: 0;
      list-style: none;
    }
    .evidence-fact {
      position: relative;
      padding-left: 12px;
      color: #1f2937;
      font-weight: 600;
      line-height: 1.55;
    }
    .evidence-fact::before {
      content: "";
      position: absolute;
      left: 0;
      top: 0.72em;
      width: 4px;
      height: 4px;
      border-radius: 999px;
      background: #64748b;
    }
    .evidence-judgement {
      margin-top: 7px;
      color: #475569;
      line-height: 1.55;
    }
    .evidence-judgement span {
      color: #64748b;
      font-size: 12px;
      font-weight: 700;
      margin-right: 6px;
    }
    .evidence-block {
      display: grid;
      gap: 14px;
    }
    .evidence-layer {
      display: grid;
      gap: 8px;
    }
    .evidence-layer-title {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      color: #0f172a;
      font-size: 12px;
      font-weight: 800;
      padding: 0 2px 2px;
      border-bottom: 1px solid #edf1f5;
    }
    .evidence-layer-title small {
      color: var(--muted);
      font-weight: 500;
      line-height: 1.35;
      text-align: right;
    }
    .evidence-section {
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      background: #fff;
      padding: 10px 11px;
      box-shadow: 0 1px 0 rgba(15, 23, 42, 0.03);
    }
    .evidence-layer.current-effective .evidence-layer-title {
      border-color: #dbe7df;
    }
    .evidence-layer.current-effective .evidence-section {
      border-color: #dbe7df;
      background: #fbfefd;
    }
    .evidence-layer.model-summary .evidence-layer-title {
      border-color: #ecd7bd;
    }
    .evidence-layer.model-summary .evidence-section {
      border-color: #ecd7bd;
      background: #fffaf3;
    }
    .evidence-layer.after-close .evidence-layer-title {
      border-color: #dde5f3;
    }
    .evidence-layer.after-close .evidence-section {
      border-color: #dde5f3;
      background: #fbfdff;
    }
    .evidence-layer.background-fact .evidence-layer-title {
      border-color: #e5e7eb;
    }
    .evidence-layer.background-fact .evidence-section {
      border-color: #e5e7eb;
      background: #fcfcfd;
    }
    .evidence-layer.historical-tag {
      opacity: 0.88;
    }
    .evidence-layer.historical-tag .evidence-section {
      border-style: dashed;
      background: #fafafa;
    }
    .evidence-section.theme {
      border-left: 3px solid var(--anchor-line);
    }
    .evidence-section.stock {
      border-left: 3px solid var(--core-line);
    }
    .evidence-section.event, .evidence-section.announcement {
      border-left: 3px solid var(--metric-change-line);
    }
    .evidence-section.summary {
      border-left: 3px solid #c4b5fd;
      background: #fbfaff;
    }
    .evidence-section.impact {
      border-left: 3px solid #f0b86a;
      background: #fffaf2;
    }
    .impact-list {
      display: grid;
      gap: 7px;
    }
    .impact-list.compact {
      gap: 5px;
    }
    .impact-item {
      display: grid;
      gap: 4px;
      padding: 7px 8px;
      border: 1px solid #f4dfbf;
      border-radius: 7px;
      background: #fffdf8;
    }
    .impact-meta {
      display: flex;
      align-items: center;
      gap: 5px;
      flex-wrap: wrap;
    }
    .impact-badge {
      border-radius: 999px;
      padding: 1px 7px;
      font-size: 12px;
      font-weight: 700;
      border: 1px solid #ead2a8;
      color: #7a5522;
      background: #fff7e8;
      line-height: 1.45;
    }
    .impact-badge.up {
      color: #8b3f36;
      border-color: #f0c7bd;
      background: #fff1ee;
    }
    .impact-badge.down {
      color: #31675b;
      border-color: #b7ded3;
      background: #edf9f5;
    }
    .impact-evidence {
      color: #374151;
      line-height: 1.65;
    }
    .theme-role-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 8px;
    }
    .theme-role-card {
      min-width: 0;
      border: 1px solid #dbe7df;
      border-radius: 8px;
      background: #fbfefd;
      padding: 9px 10px;
      display: grid;
      gap: 7px;
    }
    .theme-role-head {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 8px;
      min-width: 0;
    }
    .theme-role-title {
      min-width: 0;
      color: #0f172a;
      font-size: 13px;
      font-weight: 800;
      line-height: 1.35;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .theme-role-badge {
      flex: 0 0 auto;
      border-radius: 999px;
      border: 1px solid #b7ded3;
      background: #edf9f5;
      color: #23654b;
      padding: 1px 7px;
      font-size: 11px;
      font-weight: 800;
      line-height: 1.45;
    }
    .theme-role-badge.mid {
      border-color: #d9dce7;
      background: #f7f9fc;
      color: #475569;
    }
    .theme-role-badge.weak {
      border-color: #ead2a8;
      background: #fff7e8;
      color: #7a5522;
    }
    .theme-role-row {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
      color: #475569;
      font-size: 12px;
      line-height: 1.45;
    }
    .theme-role-row span {
      border: 1px solid #e5e7eb;
      border-radius: 999px;
      background: #fff;
      padding: 1px 7px;
      font-weight: 650;
    }
    .theme-role-reason {
      color: #334155;
      font-size: 12px;
      line-height: 1.55;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .theme-influence-list {
      display: grid;
      gap: 8px;
    }
    .theme-influence-item {
      border: 1px solid #e5e7eb;
      border-radius: 9px;
      background: #fff;
      padding: 9px;
      display: grid;
      gap: 7px;
    }
    .theme-influence-item.selected {
      border-color: #a7d8d2;
      background: #f7fcfb;
    }
    .theme-influence-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      min-width: 0;
    }
    .theme-influence-title {
      min-width: 0;
      color: #111827;
      font-size: 13px;
      font-weight: 800;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .theme-influence-metrics {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
    }
    .theme-switch-board {
      display: grid;
      gap: 8px;
    }
    .theme-switch-tabs {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
    }
    .theme-switch-card {
      appearance: none;
      border: 1px solid #e5e7eb;
      border-radius: 999px;
      background: #f8fafc;
      color: #475569;
      padding: 3px 9px;
      display: inline-flex;
      align-items: center;
      gap: 5px;
      cursor: pointer;
      font-size: 12px;
      font-weight: 800;
      line-height: 1.45;
    }
    .theme-switch-card:hover {
      border-color: #c9d5e3;
      background: #fbfdff;
    }
    .theme-switch-card.active {
      border-color: #7fb8ad;
      background: #f3fbf8;
      color: #23654b;
      box-shadow: inset 0 0 0 1px rgba(74, 139, 122, 0.12);
    }
    .theme-switch-top {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 7px;
      min-width: 0;
    }
    .theme-switch-name {
      color: inherit;
      font-size: 12px;
      font-weight: 850;
      white-space: nowrap;
      word-break: keep-all;
      line-height: 1.35;
    }
    .theme-switch-score {
      flex: 0 0 auto;
      color: inherit;
      font-size: 11px;
      font-weight: 850;
      line-height: 1.45;
    }
    .theme-switch-score::before {
      content: "·";
      margin-right: 5px;
      color: #94a3b8;
    }
    .theme-switch-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 4px;
      min-height: 18px;
    }
    .theme-switch-meta span {
      border: 1px solid #e5e7eb;
      background: #f8fafc;
      color: #64748b;
      border-radius: 999px;
      padding: 1px 6px;
      font-size: 11px;
      font-weight: 750;
      line-height: 1.45;
    }
    .theme-detail-panel {
      border: 1px solid #dbe7df;
      border-radius: 10px;
      background: linear-gradient(180deg, #fbfefd 0%, #ffffff 42%);
      padding: 10px;
      display: grid;
      gap: 10px;
    }
    .theme-detail-panel[hidden] {
      display: none;
    }
    .theme-detail-title {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 8px;
      min-width: 0;
    }
    .theme-detail-name {
      min-width: 0;
      color: #0f172a;
      font-size: 15px;
      font-weight: 900;
      line-height: 1.35;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .theme-detail-section {
      display: grid;
      gap: 6px;
    }
    .theme-detail-label {
      color: #64748b;
      font-size: 11px;
      font-weight: 850;
      letter-spacing: 0;
    }
    .theme-detail-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(112px, 1fr));
      gap: 6px;
    }
    .theme-detail-metric {
      min-width: 0;
      border: 1px solid #eef2f7;
      border-radius: 8px;
      background: #fff;
      padding: 7px 8px;
      display: grid;
      gap: 2px;
    }
    .theme-detail-metric span {
      color: #64748b;
      font-size: 11px;
      font-weight: 750;
      line-height: 1.35;
    }
    .theme-detail-metric strong {
      min-width: 0;
      color: #111827;
      font-size: 13px;
      font-weight: 850;
      line-height: 1.35;
      white-space: normal;
      word-break: break-word;
    }
    .theme-explain {
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      background: #fff;
      padding: 8px 9px;
      color: #334155;
      font-size: 12px;
      font-weight: 600;
      line-height: 1.65;
    }
    .theme-relation-row {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
    }
    .theme-relation-row span {
      border: 1px solid #e5e7eb;
      border-radius: 999px;
      background: #fff;
      color: #475569;
      padding: 2px 7px;
      font-size: 12px;
      font-weight: 700;
      line-height: 1.45;
    }
    .theme-mini-lines {
      display: grid;
      gap: 5px;
    }
    .theme-mini-line {
      min-width: 0;
      display: grid;
      grid-template-columns: 42px minmax(0, 1fr);
      gap: 6px;
      align-items: center;
      border: 1px solid #eef2f7;
      border-radius: 7px;
      background: #f8fafc;
      padding: 5px 7px;
      color: #334155;
      font-size: 12px;
      line-height: 1.45;
    }
    .theme-mini-label {
      color: #64748b;
      font-size: 11px;
      font-weight: 800;
    }
    .theme-mini-value {
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-weight: 650;
    }
    .mini-fold {
      border: 1px solid #e8e2f7;
      border-radius: 8px;
      background: #fff;
      overflow: hidden;
    }
    .mini-fold summary {
      cursor: pointer;
      list-style: none;
      padding: 7px 8px;
      color: #4c4661;
      font-size: 12px;
      font-weight: 750;
    }
    .mini-fold summary::-webkit-details-marker {
      display: none;
    }
    .mini-fold summary::after {
      content: "展开";
      float: right;
      color: #8a7ba8;
      font-weight: 650;
    }
    .mini-fold[open] summary::after {
      content: "收起";
    }
    .mini-fold-body {
      border-top: 1px solid #efeaf8;
      padding: 7px 8px;
      color: #374151;
      line-height: 1.65;
      background: #fdfcff;
    }
    .sequence-card {
      border: 1px solid #e3e8ef;
      border-radius: 8px;
      background: #ffffff;
      overflow: hidden;
    }
    .sequence-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      padding: 7px 9px;
      border-bottom: 1px solid #eef2f7;
      color: #1f2937;
      font-size: 12px;
      font-weight: 800;
    }
    .sequence-head small {
      color: #7b8495;
      font-size: 11px;
      font-weight: 650;
      white-space: nowrap;
    }
    .sequence-summary {
      display: grid;
      grid-template-columns: minmax(0, 1fr) 18px minmax(0, 1fr);
      align-items: stretch;
      gap: 6px;
      padding: 8px 9px;
      background: #f8fafc;
      border-bottom: 1px solid #eef2f7;
    }
    .sequence-summary.single {
      grid-template-columns: minmax(0, 1fr);
    }
    .sequence-summary-one {
      min-width: 0;
      padding: 6px 7px;
      border: 1px solid #e5e7eb;
      border-radius: 7px;
      background: #ffffff;
    }
    .sequence-summary-one.current {
      border-color: #a7d8d2;
      background: #f4fbfa;
    }
    .sequence-summary-label {
      display: block;
      color: #64748b;
      font-size: 11px;
      font-weight: 750;
      line-height: 1.2;
    }
    .sequence-summary-stock {
      display: block;
      margin-top: 2px;
      color: #111827;
      font-size: 12px;
      font-weight: 850;
      line-height: 1.25;
      overflow-wrap: anywhere;
    }
    .sequence-summary-meta {
      display: block;
      margin-top: 2px;
      color: #475569;
      font-size: 11px;
      font-weight: 750;
      line-height: 1.2;
    }
    .sequence-summary-arrow {
      display: flex;
      align-items: center;
      justify-content: center;
      color: #94a3b8;
      font-size: 13px;
      font-weight: 850;
    }
    .sequence-flow {
      display: grid;
      gap: 0;
    }
    .sequence-row {
      position: relative;
      display: grid;
      grid-template-columns: 32px minmax(0, 1fr) auto;
      align-items: center;
      gap: 7px;
      min-width: 0;
      padding: 6px 9px;
      border-top: 1px solid #f1f5f9;
      background: #ffffff;
    }
    .sequence-row:first-child {
      border-top: 0;
    }
    .sequence-row.current {
      background: #f2fbf9;
    }
    .sequence-row.current::before {
      content: "";
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 3px;
      background: #0f766e;
    }
    .sequence-rank {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 26px;
      height: 20px;
      border-radius: 999px;
      color: #334155;
      background: #f1f5f9;
      font-size: 11px;
      font-weight: 850;
      font-variant-numeric: tabular-nums;
    }
    .sequence-row.current .sequence-rank {
      color: #ffffff;
      background: #0f766e;
    }
    .sequence-main {
      min-width: 0;
      display: grid;
      gap: 4px;
    }
    .sequence-title-line {
      min-width: 0;
      display: flex;
      align-items: baseline;
      flex-wrap: wrap;
      gap: 4px 7px;
    }
    .sequence-name {
      color: #111827;
      font-size: 12px;
      font-weight: 800;
      line-height: 1.25;
      overflow-wrap: anywhere;
    }
    .sequence-code {
      color: #475569;
      font-size: 11px;
      font-weight: 800;
      letter-spacing: 0;
      font-variant-numeric: tabular-nums;
    }
    .sequence-meta {
      justify-self: end;
      color: #586474;
      font-size: 11px;
      font-weight: 750;
      line-height: 1.35;
      white-space: nowrap;
      padding-left: 4px;
      font-variant-numeric: tabular-nums;
    }
    .sequence-flags {
      display: flex;
      flex-wrap: wrap;
      gap: 4px;
    }
    .sequence-current-time {
      justify-self: end;
      display: grid;
      gap: 1px;
      padding: 3px 7px;
      border: 1px solid #a7d8d2;
      border-radius: 8px;
      background: #f7fcfb;
      text-align: right;
      font-variant-numeric: tabular-nums;
      white-space: nowrap;
    }
    .sequence-current-time span {
      color: #64748b;
      font-size: 10px;
      font-weight: 750;
      line-height: 1.15;
    }
    .sequence-current-time strong {
      color: #0f766e;
      font-size: 11px;
      font-weight: 900;
      line-height: 1.15;
    }
    .sequence-badge {
      display: inline-flex;
      justify-self: start;
      padding: 1px 5px;
      border-radius: 999px;
      color: #64748b;
      background: #f1f5f9;
      font-size: 10px;
      font-weight: 800;
      line-height: 1.35;
      white-space: nowrap;
    }
    .sequence-row.current .sequence-badge {
      color: #0f766e;
      background: #dff5f1;
    }
    .sequence-row.strong .sequence-badge.strong {
      color: #8a5a10;
      background: #fff1c2;
    }
    @media (max-width: 760px) {
      .sequence-summary {
        grid-template-columns: 1fr;
      }
      .sequence-summary-arrow {
        display: none;
      }
      .sequence-row {
        grid-template-columns: 30px minmax(0, 1fr) auto;
        gap: 5px;
        padding: 6px 8px;
      }
    }
    .evidence-fold {
      border: 1px solid var(--line);
      border-radius: 8px;
      background: #fbfcfe;
      overflow: hidden;
    }
    .evidence-fold summary {
      cursor: pointer;
      list-style: none;
      padding: 9px 10px;
    }
    .evidence-fold summary::-webkit-details-marker {
      display: none;
    }
    .evidence-fold .evidence-heading {
      margin-bottom: 0;
    }
    .evidence-fold summary::after {
      content: "展开";
      display: block;
      margin-top: 5px;
      color: #8a7ba8;
      font-size: 12px;
      font-weight: 650;
    }
    .evidence-fold[open] summary::after {
      content: "收起";
    }
    .evidence-fold-body {
      border-top: 1px solid var(--line);
      padding: 9px 10px;
    }
    .evidence-layer.async .evidence-section {
      background: #fdfcff;
      border-color: #e8e2f7;
    }
    .evidence-heading {
      display: flex;
      justify-content: space-between;
      gap: 8px;
      color: #0f172a;
      font-weight: 700;
      font-size: 13px;
      margin-bottom: 5px;
    }
    .evidence-source {
      color: var(--muted);
      font-size: 12px;
      font-weight: 500;
      white-space: nowrap;
    }
    .evidence-source-wrap {
      display: flex;
      align-items: center;
      justify-content: flex-end;
      gap: 6px;
      min-width: 0;
      flex-wrap: wrap;
    }
    .evidence-date-chip {
      color: #5b6472;
      background: #eef2f7;
      border: 1px solid #e2e8f0;
      border-radius: 999px;
      padding: 1px 7px;
      font-size: 11px;
      font-weight: 650;
      white-space: nowrap;
    }
    .evidence-cycle-chip {
      color: #46615d;
      background: #eef8f6;
      border: 1px solid #cfe8e3;
      border-radius: 999px;
      padding: 1px 7px;
      font-size: 11px;
      font-weight: 650;
      white-space: nowrap;
    }
    .evidence-text {
      color: #334155;
      font-size: 13px;
      line-height: 1.7;
      white-space: pre-wrap;
    }
    .evidence-gap {
      margin-top: 10px;
      color: #7a8495;
      font-size: 12px;
      line-height: 1.55;
      border-top: 1px dashed var(--line);
      padding-top: 8px;
    }
    .feed {
      display: grid;
      gap: 0;
    }
    .stream-group {
      border-bottom: 1px solid #edf2f7;
    }
    .stream-group:last-child {
      border-bottom: 0;
    }
    .stream-group.stable-rank-group {
      margin: 8px 8px 10px;
      border: 1px solid #c7d2fe;
      border-radius: 8px;
      background: #f8fbff;
      overflow: hidden;
      box-shadow: 0 8px 20px rgba(37, 99, 235, 0.08);
    }
    .stream-group.stable-rank-group + .stream-group {
      border-top: 6px solid #f1f5f9;
    }
    .stream-group-title {
      display: flex;
      align-items: center;
      gap: 8px;
      min-height: 32px;
      padding: 8px 12px 4px;
      background: #fff;
      color: var(--muted);
      font-size: 12px;
      font-weight: 600;
      font-variant-numeric: tabular-nums;
      border-left: 4px solid #dbeafe;
    }
    .stable-rank-group .stream-group-title {
      min-height: 38px;
      padding: 9px 12px;
      background: #eef6ff;
      border-left-color: var(--window);
      color: #1e3a8a;
      border-bottom: 1px solid #dbeafe;
    }
    .stable-rank-group .group-summary {
      color: #31517d;
      font-weight: 700;
    }
    .group-summary {
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      color: #546179;
      font-weight: 500;
    }
    .time {
      color: var(--muted);
      font-variant-numeric: tabular-nums;
      padding-top: 2px;
    }
    .kind {
      flex: 0 0 auto;
      border-radius: 999px;
      padding: 2px 8px;
      font-size: 12px;
      color: #fff;
      background: var(--accent);
    }
    .kind.window { background: var(--window); }
    .kind.auction { background: var(--auction); }
    .kind.scan { background: var(--scan); }
    .kind.hot_concept { background: #0f766e; }
    .kind.market_news { background: #3867b7; }
    .kind.daily_theme { background: #6f5ab8; }
    .tags {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
    }
    .tag {
      color: #374151;
      background: #f7f8fa;
      border: 1px solid #e3e7ee;
      border-radius: 999px;
      padding: 2px 8px;
      font-size: 12px;
      display: inline-flex;
      align-items: center;
      text-decoration: none;
    }
    .tag.weak {
      color: #7a8495;
      background: #fafbfc;
      border-color: #e7ebf2;
      font-weight: 500;
    }
    .stream-item {
      display: grid;
      grid-template-columns: 170px minmax(0, 1fr) auto;
      grid-template-areas:
        "title tags metric"
        "highlight highlight highlight";
      column-gap: 10px;
      row-gap: 5px;
      align-items: center;
      min-height: 54px;
      padding: 8px 12px 9px;
      background: #fff;
      border: 0;
      border-bottom: 1px solid #edf2f7;
      border-radius: 0;
      font-size: 13px;
      cursor: pointer;
    }
    .stream-item:last-child { border-bottom: 0; }
    .stream-item:hover { background: #fafcff; }
    .stream-item.stable-rank-item {
      grid-template-columns: 168px minmax(0, 1fr) auto;
      background: #fbfdff;
      border-bottom-color: #e1e9f7;
    }
    .stream-item.stable-rank-item:hover {
      background: #f4f9ff;
    }
    .stream-item.stable-rank-item.active {
      background: #eef6ff;
      box-shadow: inset 4px 0 0 var(--window);
    }
    .stream-item.stable-rank-item .stream-title {
      color: #1f3b68;
      font-weight: 800;
    }
    .stream-item.stable-rank-item .metric-chip:first-child {
      min-width: 48px;
      color: #fff;
      background: var(--window);
      border-color: var(--window);
      font-weight: 800;
    }
    .stream-item.active {
      background: #f3f8ff;
      box-shadow: inset 4px 0 0 var(--accent);
    }
    .stream-item.active .stream-title a {
      color: #174ea6;
      font-weight: 800;
    }
    .stream-item.active .stream-highlight {
      padding: 1px 6px;
      background: var(--highlight-bg);
    }
    .stream-title {
      grid-area: title;
      min-width: 0;
      font-weight: 700;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .stream-title a {
      color: inherit;
      text-decoration: none;
    }
    .stream-title a:hover {
      color: var(--accent);
      text-decoration: underline;
    }
    .stream-highlight {
      grid-area: highlight;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      color: var(--highlight-text);
      border-radius: 4px;
      padding: 0;
      font-weight: 600;
    }
    .stream-metric {
      grid-area: metric;
      display: flex;
      justify-content: flex-end;
      gap: 5px;
    }
    .stream-tags {
      grid-area: tags;
      display: flex;
      flex-wrap: nowrap;
      gap: 4px;
      min-width: 0;
      overflow: hidden;
      align-items: center;
    }
    .stream-tags .tag {
      padding: 1px 7px;
    }
    .tag.role {
      border-color: var(--leader-line);
      background: var(--leader-bg);
      color: var(--leader-text);
      font-weight: 700;
    }
    .tag.core {
      border-color: var(--core-line);
      background: var(--core-bg);
      color: var(--core-text);
      font-weight: 700;
    }
    .tag.anchor {
      border-color: var(--anchor-line);
      background: var(--anchor-bg);
      color: var(--anchor-text);
      font-weight: 700;
    }
    a.tag:hover {
      filter: brightness(0.98);
      text-decoration: underline;
    }
    .empty, .error {
      background: #fff;
      border: 1px solid var(--line);
      border-radius: 7px;
      padding: 18px;
      color: var(--muted);
    }
    .error { color: #b42318; }
    @media (max-width: 640px) {
      .topbar {
        height: auto;
        min-height: 52px;
        align-items: flex-start;
        flex-direction: column;
        padding: 9px 12px;
        gap: 4px;
      }
      .meta { flex-wrap: wrap; gap: 7px; }
      main { width: calc(100vw - 20px); padding-top: 10px; }
      .intel-layout {
        grid-template-columns: 1fr;
      }
      .detail-panel {
        position: static;
        height: auto;
        max-height: none;
      }
      #detailPanel {
        overflow: visible;
      }
      .detail-body {
        height: auto;
        max-height: none;
        overflow: visible;
      }
      .evidence-focus-grid {
        grid-template-columns: 1fr;
      }
      .stream-item {
        grid-template-columns: minmax(0, 1fr) auto;
        grid-template-areas:
          "title metric"
          "tags tags"
          "highlight highlight";
      }
      .stream-title {
        max-width: none;
      }
      .metric-chip {
        min-width: 58px;
      }
      .time { font-size: 12px; }
    }
  </style>
</head>
<body>
  <header class="topbar">
    <h1>异动情报引擎</h1>
    <div class="meta">
      <a class="nav-link" href="/market-width">市场概览</a>
      <a class="nav-link" href="/leaders">领头羊</a>
      <span><span class="dot"></span> 自动刷新</span>
      <span id="refreshText">等待数据</span>
    </div>
  </header>
  <main>
    <div class="toolbar">
      <select class="date-picker" id="tradeDateSelect" aria-label="选择交易日"></select>
      <button class="toolbtn" id="latestBtn" type="button">最近交易日</button>
    </div>
    <div class="summarybar" id="summarybar"></div>
    <section class="intel-layout">
      <section class="feed-panel">
        <div class="feed-title">异动情报流 <small>近5日涨停 + 5日涨幅Top30研究池，按时间滚动</small></div>
        <section class="feed" id="feed"></section>
      </section>
      <aside class="detail-panel">
        <div class="detail-title">证据详情 <small id="detailTime"></small></div>
        <div id="detailPanel"></div>
      </aside>
    </section>
  </main>
  <script>
    const $ = id => document.getElementById(id);
    const clean = value => String(value ?? "").replace(/[ \t\r\f\v]+/g, " ").trim();
    const esc = value => clean(value).replace(/[&<>"']/g, c => ({
      "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;"
    }[c]));
    const escBlock = value => String(value ?? "").replace(/[&<>"']/g, c => ({
      "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;"
    }[c]));
    function parseTags(value) {
      if (Array.isArray(value)) return value;
      if (!value) return [];
      try { return JSON.parse(value); } catch { return String(value).split(/[,\s，、]+/); }
    }
    let currentFeed = [];
    let selectedKey = "";
    const detailCache = new Map();
    const activeDetailTheme = new Map();
    let detailRequestSeq = 0;
    function rowKey(row) {
      return [row.kind, row.event_time, row.code, row.title].map(clean).join("|");
    }
    function stockUrl(code) {
      return `https://stockpage.10jqka.com.cn/${encodeURIComponent(clean(code))}/`;
    }
    function stockAnchorHtml(row) {
      const title = row.title || `${row.name || ""} ${row.code || ""}`;
      const code = clean(row.code);
      if (/^\d{6}$/.test(code)) {
        return `<a href="${stockUrl(code)}" target="_blank" rel="noopener noreferrer">${esc(title)}</a>`;
      }
      return esc(title);
    }
    function stockTitleText(row) {
      return clean(row?.title || `${row?.name || ""} ${row?.code || ""}`) || "未选择";
    }
    function tagValue(tags, prefix) {
      const hit = tags.find(tag => tag.startsWith(prefix));
      return hit ? hit.slice(prefix.length) : "";
    }
    function anchorText(row) {
      const tags = parseTags(row.tags).map(clean).filter(Boolean);
      return tagValue(tags, "锚点:") || "未锚定";
    }
    function headlineThemes(row) {
      return parseTags(row.tags)
        .map(clean)
        .filter(tag => tag && !tag.includes(":") && !tag.startsWith("Top") && !tag.startsWith("扫描Top") && !tag.startsWith("窗口Top"))
        .filter(Boolean)
        .filter((tag, index, arr) => arr.indexOf(tag) === index);
    }
    function timeText(value) {
      if (!value) return "--:--:--";
      const [date, time] = String(value).split(" ");
      return time || date || "--:--:--";
    }
    function serviceChips(data) {
      const ctx = data.service_context || {};
      const serviceDate = ctx.service_trade_date || data.trade_date || "最近交易日";
      const chips = [
        `${serviceDate} ${ctx.phase_label || "观察"}`,
        ctx.base_trade_date ? `底稿 ${ctx.base_trade_date} 收盘` : "",
        ctx.latest_realtime_at ? `实时 ${timeText(ctx.latest_realtime_at)}` : "",
        ctx.root_cache_updated_at ? `证据缓存 ${timeText(ctx.root_cache_updated_at)}` : "",
        ctx.post_close_status_label ? `盘后 ${ctx.post_close_status_label}` : ""
      ];
      return chips.filter(Boolean);
    }
    function renderSummary(data) {
      const status = data.status || {};
      const windowInfo = data.window || {};
      const feedMeta = data.feed_meta || {};
      const tradeDate = data.trade_date || status.trade_date || "最近交易日";
      const dataAlerts = Array.isArray(status.data_source_alerts) ? status.data_source_alerts : [];
      $("summarybar").innerHTML = [
        ...serviceChips(data),
        `扫描 ${status.scan_count_today || 0}`,
        `窗口 ${status.window_count_today || 0}`,
        `情报 ${feedMeta.count || 0}`,
        `有效锚点 ${status.active_anchors || 0}`,
        `最新 ${status.latest_scan_at || "-"}`,
        windowInfo.window_id ? `当前窗口 ${windowInfo.window_id}` : "",
        dataAlerts.length ? `数据源异常 ${dataAlerts.length}` : ""
      ].filter(Boolean).map(x => `<span>${esc(x)}</span>`).join("");
    }
    const EVIDENCE_LAYER_META = {
      realtime: { key: "实时证据", title: "实时链路证据", hint: "随扫描即时产生", className: "realtime" },
      async: { key: "异步证据", title: "有效事实总结", hint: "近10日有效事实", className: "async" }
    };
    const EVIDENCE_TYPE_META = {
      facts: { label: "有效事实总结", className: "stock", source: "有效事实", priority: 0, limit: 260, maxItems: 12 },
      move: { label: "异动解释", className: "summary", source: "模型解释", priority: 1, limit: 100, maxItems: 1 },
      quality: { label: "异动质量", className: "event", source: "模型判断", priority: 2, limit: 100, maxItems: 1 },
      period: { label: "区间领头", className: "theme", source: "研究池区间结构", priority: 3, limit: 120, maxItems: 3 },
      influence: { label: "带动性", className: "event", source: "同锚扩散", priority: 4, limit: 140, maxItems: 8 },
      lhb: { label: "龙虎榜席位", className: "event", source: "F10重要事件", priority: 30, limit: 130, maxItems: 2 },
      anchor: { label: "锚点一致性", className: "theme", source: "模型判断", priority: 3, limit: 120, maxItems: 1 },
      support: { label: "核心支撑", className: "stock", source: "模型筛选", priority: 4, limit: 110, maxItems: 2 },
      counter: { label: "瑕疵", className: "announcement", source: "模型判断", priority: 6, limit: 110, maxItems: 1 },
      final: { label: "核心结论", className: "summary", source: "模型结论", priority: 5, limit: 100, maxItems: 1 },
      timeliness: { label: "时效判断", className: "event", source: "模型判断", priority: 8, limit: 120, maxItems: 1 },
      flaw: { label: "最大瑕疵", className: "announcement", source: "事实卡", priority: 8, limit: 120, maxItems: 1 },
      gap: { label: "证据缺口", className: "event", source: "事实卡", priority: 9, limit: 110, maxItems: 3 },
      core: { label: "核心证据", className: "stock", source: "过滤后证据", priority: 9, limit: 130, maxItems: 3 },
      impact: { label: "有效事实总结", className: "impact", source: "有效事实", priority: 10, limit: 260, maxItems: 12 },
      summary: { label: "异步总结", className: "summary", source: "模型总结", priority: 20, limit: 120, maxItems: 1 },
      headline_theme_roles: { label: "头条题材角色", className: "theme", source: "研究池题材角色", priority: 21, limit: 160, maxItems: 8 },
      realtime: { label: "实时判断", className: "theme", source: "实时扫描", priority: 25, limit: 160, maxItems: 1 },
      theme: { label: "题材证据", className: "theme", source: "题材解释", priority: 30, limit: 160, maxItems: 2 },
      stock: { label: "个股证据", className: "stock", source: "个股解释", priority: 40, limit: 160, maxItems: 2 },
      announcement: { label: "公告", className: "announcement", source: "公告", priority: 80, limit: 120, maxItems: 1 },
      event: { label: "事件", className: "event", source: "事件", priority: 90, limit: 120, maxItems: 1 }
    };
    const EVIDENCE_LABEL_TYPE = {
      "关键事实": "facts",
      "事实时间线": "facts",
      "有效事实总结": "facts",
      "异动解释": "move",
      "异动质量": "quality",
      "区间领头": "period",
      "带动性": "influence",
      "龙虎榜席位": "lhb",
      "锚点一致性": "anchor",
      "核心支撑": "support",
      "持续依据": "support",
      "瑕疵": "counter",
      "核心结论": "final",
      "时效判断": "timeliness",
      "最大瑕疵": "flaw",
      "证据缺口": "gap",
      "核心证据": "core",
      "影响要素": "impact",
      "异步总结": "summary",
      "头条题材角色": "headline_theme_roles",
      "实时判断": "realtime",
      "题材证据": "theme",
      "题材": "theme",
      "个股证据": "stock",
      "公告": "announcement",
      "事件": "event"
    };
    function evidenceMeta(partOrType) {
      const raw = typeof partOrType === "string" ? partOrType : (partOrType.type || EVIDENCE_LABEL_TYPE[partOrType.label] || "");
      return EVIDENCE_TYPE_META[raw] || EVIDENCE_TYPE_META.event;
    }
    function tagText(tag) {
      const text = clean(tag).replace(/\|\d{6}$/, "");
      return text.startsWith("头条题材:") ? text.slice("头条题材:".length) : text;
    }
    function tagStockCode(tag) {
      const match = clean(tag).match(/\|(\d{6})$/);
      return match ? match[1] : "";
    }
    function tagClass(tag) {
      const text = tagText(tag);
      if (clean(tag).startsWith("头条题材:") || (!text.includes(":") && !text.startsWith("Top") && !text.startsWith("扫描Top") && !text.startsWith("窗口Top"))) return "tag anchor";
      if (text.startsWith("锚点:")) return "tag anchor";
      if (["领涨", "领涨中军", "先锋", "高标"].includes(text)) return "tag role";
      if (text.startsWith("领涨:") || text.startsWith("全池领涨:") || text.startsWith("局部领涨:")) return "tag role";
      if (text === "中军" || text.startsWith("中军:") || text.startsWith("全池中军:") || text.startsWith("局部中军:")) return "tag core";
      if (["孤立脉冲", "锚点成员"].includes(text) || text.startsWith("出现:")) return "tag weak";
      return "tag";
    }
    function tagHtml(tag) {
      const text = tagText(tag);
      const code = tagStockCode(tag);
      const cls = tagClass(tag);
      if (code && /^(领涨|全池领涨|局部领涨|中军|全池中军|局部中军):/.test(text)) {
        return `<a class="${cls}" href="${stockUrl(code)}" target="_blank" rel="noopener noreferrer">${esc(text)}</a>`;
      }
      return `<span class="${cls}">${esc(text)}</span>`;
    }
    function tagPriority(tag) {
      const text = tagText(tag);
      if (clean(tag).startsWith("头条题材:") || (!text.includes(":") && !text.startsWith("Top") && !text.startsWith("扫描Top") && !text.startsWith("窗口Top"))) return 5;
      if (text.startsWith("锚点:")) return 10;
      if (["领涨", "领涨中军", "中军", "先锋", "高标", "孤立脉冲", "锚点成员"].includes(text)) return 20;
      if (/^(领涨|全池领涨|局部领涨):/.test(text)) return 30;
      if (/^(中军|全池中军|局部中军):/.test(text)) return 40;
      if (text.startsWith("出现:")) return 50;
      return 100;
    }
    function isStrongTag(tag) {
      const text = tagText(tag);
      if (clean(tag).startsWith("头条题材:") || (!text.includes(":") && !text.startsWith("Top") && !text.startsWith("扫描Top") && !text.startsWith("窗口Top"))) return true;
      if (/^(领涨|全池领涨|局部领涨|中军|全池中军|局部中军):/.test(text)) return false;
      if (["领涨", "领涨中军", "中军"].includes(text)) return false;
      if (text.startsWith("锚点:")) return !text.includes("未锚定");
      if (["领涨", "领涨中军", "中军", "先锋", "高标", "孤立脉冲", "锚点成员"].includes(text)) return true;
      if (/^(领涨|全池领涨|局部领涨|中军|全池中军|局部中军):/.test(text)) return true;
      return false;
    }
    function visibleTagsFor(row) {
      const tags = parseTags(row.tags).map(clean).filter(Boolean);
      const anchor = anchorText(row);
      const visible = [
        ...tags.filter(tag => clean(tag).startsWith("头条题材:") || (!tagText(tag).includes(":") && !tagText(tag).startsWith("Top") && !tagText(tag).startsWith("扫描Top") && !tagText(tag).startsWith("窗口Top"))),
        anchor && anchor !== "未锚定" && !tags.some(tag => clean(tag).startsWith("头条题材:") || (!tagText(tag).includes(":") && !tagText(tag).startsWith("Top") && !tagText(tag).startsWith("扫描Top") && !tagText(tag).startsWith("窗口Top"))) ? `锚点:${anchor}` : "",
        ...tags
      ].filter(Boolean)
        .filter(tag => tagText(tag) !== "证据:pending")
        .filter(isStrongTag)
        .filter((tag, index, arr) => arr.indexOf(tag) === index)
        .sort((a, b) => tagPriority(a) - tagPriority(b));
      const headline = visible.filter(tag => clean(tag).startsWith("头条题材:") || (!tagText(tag).includes(":") && !tagText(tag).startsWith("Top") && !tagText(tag).startsWith("扫描Top") && !tagText(tag).startsWith("窗口Top")));
      return headline.length ? headline : visible.slice(0, 8);
    }
    function normalizeEvidenceRaw(row) {
      let raw = String(row.detail ?? "").replace(/\r/g, "").trim();
      raw = raw.replace(/^【亮点】[^\n]*(\n|$)/, "");
      const marker = raw.search(/【实时证据】|【异步证据】|异步总结：|有效事实总结：|事实时间线：|影响要素：|题材证据：|个股证据：|事件：|公告：|题材：/);
      if (marker >= 0) raw = raw.slice(marker);
      else raw = "";
      return raw.replace(/\n{3,}/g, "\n\n").trim();
    }
    function evidenceType(label) {
      return EVIDENCE_LABEL_TYPE[label] || "event";
    }
    function evidenceSource(label, body) {
      const meta = EVIDENCE_TYPE_META[evidenceType(label)] || EVIDENCE_TYPE_META.event;
      if (["事实时间线", "有效事实总结", "影响要素", "异步总结", "实时判断", "题材证据", "个股证据", "公告", "事件"].includes(label)) return meta.source;
      if (/互动易/.test(body)) return "互动易";
      if (/公告/.test(body) || label === "公告") return "公告";
      if (/年报|半年报|季报/.test(body)) return "定期报告";
      if (/官微|公众号/.test(body)) return "官微";
      return meta.source;
    }
    function parseJsonArray(value) {
      if (Array.isArray(value)) return value;
      if (!value) return [];
      if (typeof value === "string") {
        try {
          const parsed = JSON.parse(value);
          return Array.isArray(parsed) ? parsed : [];
        } catch {
          return [];
        }
      }
      return [];
    }
    function parseJsonObject(value) {
      if (value && typeof value === "object" && !Array.isArray(value)) return value;
      if (!value || typeof value !== "string") return {};
      try {
        const parsed = JSON.parse(value);
        return parsed && typeof parsed === "object" && !Array.isArray(parsed) ? parsed : {};
      } catch {
        return {};
      }
    }
    function evidenceView(row) {
      const view = parseJsonObject(row.evidence_view);
      return Number(view.schema_version) >= 1 ? view : {};
    }
    function normalizeEvidenceLayer(layer) {
      const text = clean(layer);
      if (text === "async" || text === "异步证据" || text === "异步补充证据") return "异步证据";
      return "实时证据";
    }
    function isDeprecatedRankEvidence(item) {
      const label = clean(item.label || "");
      const type = clean(item.type || "");
      const source = clean(item.source || "");
      const sourceTable = clean(item.source_table || "");
      const body = clean(item.body || item.text || "");
      return sourceTable === "stock_period_rankings"
        || type === "period"
        || label === "区间领头"
        || source.includes("问财")
        || body.includes("问财");
    }
    function normalizeEvidenceItem(item) {
      if (isDeprecatedRankEvidence(item)) return null;
      const label = clean(item.label || "");
      const displayLines = parseJsonArray(item.display_lines).map(clean).filter(Boolean);
      const body = clean(item.body || item.text || displayLines.join("\n"));
      if (!label || (!body && !displayLines.length)) return null;
      return {
        layer: normalizeEvidenceLayer(item.layer),
        label,
        body,
        display_lines: displayLines,
        source: clean(item.source || ""),
        type: clean(item.type || evidenceType(label)),
        priority: Number(item.priority ?? evidenceMeta(evidenceType(label)).priority),
        payload: item.payload ?? null,
        structured_payload: item.structured_payload ?? null
      };
    }
    function clampText(text, limit) {
      const value = clean(text);
      if (value.length <= limit) return value;
      return `${value.slice(0, Math.max(0, limit - 1))}…`;
    }
    function conciseEvidenceBody(label, body) {
      const lines = String(body || "").split(/\n+/).map(clean).filter(Boolean);
      const deduped = lines.filter((line, index, arr) => arr.indexOf(line) === index);
      const meta = evidenceMeta(evidenceType(label));
      if (meta.maxItems <= 1) return clampText(deduped.join("；"), meta.limit);
      return deduped.slice(0, meta.maxItems).map(line => clampText(line, meta.limit)).join("\n");
    }
    function evidencePartPriority(part) {
      if (part.layer === "异步证据") {
        const preferred = {
          "关键事实": 0,
          "事实时间线": 0,
          "有效事实总结": 0,
          "区间领头": 2,
          "带动性": 4,
          "持续依据": 5,
          "最大瑕疵": 4,
          "证据缺口": 5,
          "持续性": 6,
          "异动解释": 7,
          "核心证据": 20,
          "影响要素": 21,
          "核心支撑": 30,
          "瑕疵": 31,
          "时效判断": 32,
          "核心结论": 40,
          "异动质量": 41,
          "锚点一致性": 42,
          "异步总结": 50
        };
        if (Object.prototype.hasOwnProperty.call(preferred, part.label)) return preferred[part.label];
      }
      if (Number.isFinite(Number(part.priority))) return Number(part.priority);
      return evidenceMeta(part).priority || 100;
    }
    function isUsefulDecisionPart(part) {
      const useful = new Set(["事实时间线", "有效事实总结", "关键事实", "区间领头", "带动性", "持续依据", "最大瑕疵", "证据缺口", "持续性"]);
      if (!useful.has(part.label)) return false;
      if (part.label === "持续性" && /走弱|风险-[1-9]/.test(part.body || "")) return true;
      if (part.label === "持续性") return false;
      if (part.label === "证据缺口" && !clean(part.body)) return false;
      return true;
    }
    function curateEvidenceParts(parts, layer) {
      let scoped = parts.filter(part => part.layer === layer);
      if (layer === "实时证据" && parts.some(part => part.layer === "异步证据" && isUsefulDecisionPart(part))) {
        scoped = scoped.filter(part => part.label === "头条题材角色");
        if (!scoped.length) return [];
      }
      if (layer === "实时证据" && scoped.some(part => part.label === "头条题材角色")) {
        scoped = scoped.filter(part => part.label !== "领涨中军");
      }
      if (layer === "异步证据") {
        const decisionParts = scoped.filter(isUsefulDecisionPart);
        if (decisionParts.length) {
          return decisionParts
            .map(part => ({ ...part, body: conciseEvidenceBody(part.label, part.body) }))
            .filter(part => part.body)
            .sort((a, b) => evidencePartPriority(a) - evidencePartPriority(b))
            .slice(0, 6);
        }
      }
      const hasImpact = scoped.some(part => part.label === "影响要素");
      const hasCore = scoped.some(part => part.label === "核心证据");
      const hasJudgement = hasImpact || scoped.some(part => part.label === "异步总结");
      if (layer === "异步证据" && hasImpact) {
        scoped = scoped.filter(part => part.label !== "异步总结");
      }
      if (layer === "异步证据" && (hasCore || hasImpact)) {
        scoped = scoped.filter(part => !["核心结论", "异动质量", "锚点一致性"].includes(part.label));
      }
      if (layer === "异步证据" && hasJudgement) {
        scoped = scoped.filter(part => !["公告", "事件", "题材"].includes(part.label));
      }
      return scoped
        .map(part => ({ ...part, body: conciseEvidenceBody(part.label, part.body) }))
        .filter(part => part.body)
        .sort((a, b) => evidencePartPriority(a) - evidencePartPriority(b))
        .slice(0, layer === "异步证据" ? 6 : 1);
    }
    function impactLineHtml(line) {
      const match = clean(line).match(/^([^/：]+)(?:\/([^/：]+))?(?:\/([^：]+))?：(.+)$/);
      if (!match) return `<div class="impact-item"><div class="impact-evidence">${esc(line)}</div></div>`;
      const factor = match[1] || "影响";
      const direction = match[2] || "";
      const importance = match[3] || "";
      const evidence = match[4] || "";
      const dirClass = direction.includes("正") ? "up" : direction.includes("负") ? "down" : "";
      return `<div class="impact-item">
        <div class="impact-meta">
          <span class="impact-badge">${esc(factor)}</span>
          ${direction ? `<span class="impact-badge ${dirClass}">${esc(direction)}</span>` : ""}
          ${importance ? `<span class="impact-badge">${esc(importance)}</span>` : ""}
        </div>
        <div class="impact-evidence">${esc(evidence)}</div>
      </div>`;
    }
    function sequenceNode(raw) {
      const text = clean(raw);
      if (!text) return null;
      const orderMatch = text.match(/^(\d+)\s+(.+)$/);
      const order = orderMatch ? orderMatch[1] : "";
      const rest = orderMatch ? orderMatch[2] : text;
      const isCurrent = /(^|\s)本股\s/.test(rest);
      const isStrong = /强势榜/.test(rest);
      const timeMatch = rest.match(/(\d{2}:\d{2}:\d{2})$/);
      const countMatch = rest.match(/\sx\d+$/);
      const meta = clean((timeMatch ? timeMatch[1] : countMatch ? countMatch[0].trim() : ""));
      let name = rest
        .replace(/^本股\s+/, "")
        .replace(/\((强势榜|非强势)\)/g, "")
        .replace(/(\d{2}:\d{2}:\d{2})$/, "")
        .replace(/\sx\d+$/, "")
        .trim();
      const codeMatch = name.match(/\b(\d{6})$/);
      const code = codeMatch ? codeMatch[1] : "";
      if (code) name = name.replace(/\b\d{6}$/, "").trim();
      return { order, name: name || rest, code, meta, isCurrent, isStrong, raw: text };
    }
    function sequenceNodeHtml(node, title = "") {
      if (!node) return "";
      const tags = [
        node.isCurrent ? `<span class="sequence-badge">本股</span>` : "",
        node.isStrong ? `<span class="sequence-badge strong">强势</span>` : ""
      ].filter(Boolean).join("");
      const isTimeSequence = title === "时间序列";
      const meta = node.meta
        ? node.isCurrent && isTimeSequence
          ? `<div class="sequence-current-time"><span>本股时间</span><strong>${esc(node.meta)}</strong></div>`
          : `<div class="sequence-meta">${esc(node.meta)}</div>`
        : `<span></span>`;
      return `<div class="sequence-row ${node.isCurrent ? "current" : ""} ${node.isStrong ? "strong" : ""}">
        ${node.order ? `<span class="sequence-rank">${esc(node.order)}</span>` : `<span></span>`}
        <div class="sequence-main">
          <div class="sequence-title-line">
            <span class="sequence-name" title="${esc([node.name, node.code].filter(Boolean).join(' '))}">${esc(node.name)}</span>
            ${node.code ? `<span class="sequence-code">${esc(node.code)}</span>` : ""}
          </div>
          ${tags ? `<div class="sequence-flags">${tags}</div>` : ""}
        </div>
        ${meta}
      </div>`;
    }
    function sequenceSummaryHtml(title, nodes) {
      if (!nodes.length) return "";
      const leader = nodes[0];
      const current = nodes.find(node => node.isCurrent) || nodes[nodes.length - 1];
      const leaderLabel = title === "时间序列" ? "首发" : "榜首";
      const currentLabel = current?.isCurrent ? "本股" : "末位";
      const one = (label, node, cls = "") => node ? `<div class="sequence-summary-one ${cls}">
        <span class="sequence-summary-label">${esc(label)} ${node.order ? `#${esc(node.order)}` : ""}</span>
        <span class="sequence-summary-stock">${esc(node.name)}${node.code ? ` ${esc(node.code)}` : ""}</span>
        ${node.meta ? `<span class="sequence-summary-meta">${esc(node.meta)}</span>` : ""}
      </div>` : "";
      if (!leader || !current) return "";
      if (leader === current) {
        return `<div class="sequence-summary single">${one(leaderLabel, leader, leader.isCurrent ? "current" : "")}</div>`;
      }
      return `<div class="sequence-summary">
        ${one(leaderLabel, leader, leader.isCurrent ? "current" : "")}
        <div class="sequence-summary-arrow">&rarr;</div>
        ${one(currentLabel, current, current.isCurrent ? "current" : "")}
      </div>`;
    }
    function sequenceFlowHtml(title, rawBody) {
      const nodes = rawBody.split("；").map(sequenceNode).filter(Boolean);
      return sequenceFlowFromNodesHtml(title, nodes);
    }
    function sequenceFlowFromNodesHtml(title, nodes) {
      if (!nodes.length) return "";
      const hint = title === "时间序列" ? "触发先后" : "出现频次";
      return `<section class="sequence-card">
        <div class="sequence-head"><span>${esc(title)}</span><small>${esc(hint)} · ${nodes.length}条</small></div>
        ${sequenceSummaryHtml(title, nodes)}
        <div class="sequence-flow">${nodes.map(node => sequenceNodeHtml(node, title)).join("")}</div>
      </section>`;
    }
    function structuredSequenceNodes(items, active = false) {
      return (Array.isArray(items) ? items : []).map(item => ({
        order: clean(item.rank || item.order || ""),
        name: clean(item.name || item.stock_name || "未知"),
        code: clean(item.code || ""),
        meta: active ? (Number(item.count) ? `x${Number(item.count)}` : "") : clean(item.time || ""),
        isCurrent: Boolean(item.current || item.is_current),
        isStrong: Boolean(item.strong || item.is_strong),
        raw: ""
      })).filter(item => item.name || item.code);
    }
    function structuredInfluenceHtml(payload, fallbackLines) {
      const data = parseJsonObject(payload);
      if (!Object.keys(data).length) return "";
      const lines = Array.isArray(data.lines) ? data.lines.map(clean).filter(Boolean) : fallbackLines;
      const headline = lines
        .filter(line => !line.startsWith("时间序列：") && !line.startsWith("活跃序列：") && !line.startsWith("扩散股：") && !line.startsWith("扩散："))
        .slice(0, 4);
      const timeNodes = structuredSequenceNodes(data.time_sequence, false);
      const activeNodes = structuredSequenceNodes(data.active_sequence, true);
      const spread = data.spread && typeof data.spread === "object" ? data.spread : {};
      const spreadLine = clean(spread.label ? `扩散：3分钟+${Number(spread.m3 || 0)}，5分钟+${Number(spread.m5 || 0)}，10分钟+${Number(spread.m10 || 0)}；${spread.label}` : "");
      return [
        headline.length ? `<div class="evidence-chip-row">${headline.map(influenceLineHtml).join("")}</div>` : "",
        spreadLine ? `<div class="evidence-chip-row">${influenceLineHtml(spreadLine)}</div>` : "",
        sequenceFlowFromNodesHtml("时间序列", timeNodes),
        sequenceFlowFromNodesHtml("活跃序列", activeNodes)
      ].filter(Boolean).join("");
    }
    function influenceToneClass(label, score = 0) {
      const text = clean(label);
      if (/强|首发/.test(text) || Number(score || 0) >= 16) return "good";
      if (/中|扩散/.test(text) || Number(score || 0) >= 10) return "warn";
      return "weak";
    }
    function miniNodeText(node) {
      if (!node) return "";
      const rank = clean(node.order || node.rank || "");
      const stock = [clean(node.name), clean(node.code)].filter(Boolean).join(" ");
      const meta = clean(node.meta || "");
      return `${rank ? `#${rank} ` : ""}${stock}${meta ? ` ${meta}` : ""}`;
    }
    function miniSequenceLine(label, nodes) {
      if (!Array.isArray(nodes) || !nodes.length) return "";
      const first = nodes[0];
      const current = nodes.find(node => node.isCurrent) || nodes[nodes.length - 1];
      const value = first === current ? miniNodeText(first) : `${miniNodeText(first)} → ${miniNodeText(current)}`;
      return value ? `<div class="theme-mini-line"><span class="theme-mini-label">${esc(label)}</span><span class="theme-mini-value" title="${esc(value)}">${esc(value)}</span></div>` : "";
    }
    function multiThemeInfluenceHtml(items) {
      const rows = (Array.isArray(items) ? items : [])
        .filter(item => item && typeof item === "object" && clean(item.theme_name || item.anchor))
        .sort((a, b) => {
          const selected = Number(Boolean(b.selected)) - Number(Boolean(a.selected));
          if (selected) return selected;
          return Number(b.influence || 0) - Number(a.influence || 0)
            || Number(a.theme_rank || 999) - Number(b.theme_rank || 999);
        })
        .slice(0, 8);
      if (!rows.length) return "";
      return `<div class="theme-influence-list">${rows.map(item => {
        const payload = parseJsonObject(item.payload || item.influence_payload || {});
        const spread = payload.spread && typeof payload.spread === "object" ? payload.spread : {};
        const label = clean(item.influence_label || payload.label || "观察");
        const tone = influenceToneClass(label, item.influence);
        const theme = clean(item.theme_name || item.anchor);
        const trigger = Number(item.wave_trigger_order || payload.position?.trigger_order || 0);
        const waveNo = Number(item.wave_no || payload.position?.wave_no || 1);
        const delay = clean(payload.position?.delay_text || "");
        const preheat = clean(payload.preheat?.label || "");
        const first = payload.first && typeof payload.first === "object" ? payload.first : {};
        const firstText = [clean(first.name), clean(first.code), clean(first.time)].filter(Boolean).join(" ");
        const m3 = Number(item.new_after_3m ?? spread.m3 ?? 0);
        const m5 = Number(item.new_after_5m ?? spread.m5 ?? 0);
        const m10 = Number(item.new_after_10m ?? spread.m10 ?? 0);
        const metrics = [
          `<span class="evidence-chip ${tone}">${esc(label)}</span>`,
          trigger ? `<span class="evidence-chip weak">${esc(waveNo > 1 ? `第${waveNo}波 #${trigger}` : `触发 #${trigger}`)}</span>` : "",
          `<span class="evidence-chip ${m3 >= 3 || m10 >= 8 ? "good" : m10 >= 4 ? "warn" : "weak"}">扩散 3m+${esc(m3)} / 5m+${esc(m5)} / 10m+${esc(m10)}</span>`,
          preheat ? `<span class="evidence-chip ${preheat.includes("冷") ? "good" : "weak"}">${esc(preheat)}</span>` : "",
          delay && trigger > 1 ? `<span class="evidence-chip weak">晚于首发 ${esc(delay)}</span>` : ""
        ].filter(Boolean).join("");
        const timeNodes = structuredSequenceNodes(payload.time_sequence, false);
        const activeNodes = structuredSequenceNodes(payload.active_sequence, true);
        const seq = [
          miniSequenceLine("时间", timeNodes),
          miniSequenceLine("活跃", activeNodes)
        ].filter(Boolean).join("");
        return `<article class="theme-influence-item ${item.selected ? "selected" : ""}">
          <div class="theme-influence-head">
            <div class="theme-influence-title" title="${esc(theme)}">${esc(theme)}</div>
            <span class="theme-role-badge${tone === "good" ? "" : tone === "warn" ? " weak" : " mid"}">${esc(Number(item.influence || 0).toFixed(0))}分</span>
          </div>
          <div class="theme-influence-metrics">${metrics}</div>
          ${firstText ? `<div class="theme-role-row"><span>首发 ${esc(firstText)}</span></div>` : ""}
          ${seq ? `<div class="theme-mini-lines">${seq}</div>` : ""}
        </article>`;
      }).join("")}</div>`;
    }
    function influenceLineHtml(line) {
      const text = clean(line);
      if (!text) return "";
      if (text.startsWith("时间序列：") || text.startsWith("活跃序列：")) {
        const [title, ...rest] = text.split("：");
        const rawBody = rest.join("：");
        return sequenceFlowHtml(title, rawBody);
      }
      if (text.startsWith("扩散股：")) {
        const [title, ...rest] = text.split("：");
        const body = rest.join("：");
        return `<details class="mini-fold">
          <summary>${esc(title)}</summary>
          <div class="mini-fold-body">${esc(body || text)}</div>
        </details>`;
      }
      const cls = /强扩散|冷启动|首发质量：强|疑似带动强|具备/.test(text)
        ? "good"
        : /热启动|晚启动|后排|弱|缺|不足/.test(text)
        ? "warn"
        : "weak";
      const labelMatch = text.match(/^([^：；]+)[：；](.+)$/);
      if (labelMatch) {
        return `<span class="evidence-chip ${cls}">${esc(labelMatch[1])}：${esc(clampText(labelMatch[2], 80))}</span>`;
      }
      return `<span class="evidence-chip ${cls}">${esc(clampText(text, 90))}</span>`;
    }
    function themeRoleBadgeClass(strength) {
      const text = clean(strength);
      if (text === "强") return "";
      if (text === "中") return " mid";
      return " weak";
    }
    function themeRoleStock(name, code) {
      const label = [clean(name), clean(code)].filter(Boolean).join(" ");
      return label || "";
    }
    function themeRoleCardsHtml(part) {
      const roles = (Array.isArray(part.payload) ? part.payload : [])
        .filter(item => item && typeof item === "object" && clean(item.theme_name))
        .sort((a, b) => Number(a.theme_rank || 999) - Number(b.theme_rank || 999) || Number(b.match_score || 0) - Number(a.match_score || 0));
      if (!roles.length) return "";
      return `<div class="theme-role-grid">${roles.slice(0, 8).map(item => {
        const strength = clean(item.explain_strength || "弱");
        const role = clean(item.role_label || "题材成员");
        const memberCount = Number(item.member_count || 0);
        const leader = themeRoleStock(item.leader_name, item.leader_code);
        const core = themeRoleStock(item.core_name, item.core_code);
        const leaderRank = Number(item.rank_leader || 0);
        const coreRank = Number(item.rank_core || 0);
        const changeNumber = Number(item.pct_change);
        const change = Number.isFinite(changeNumber) && Math.abs(changeNumber) >= 0.01 ? `${changeNumber.toFixed(2)}%` : "";
        const reason = clampText(clean(item.reason_explain || item.concept_name || ""), 128);
        const meta = [
          role ? `<span>${esc(role)}</span>` : "",
          memberCount ? `<span>成员 ${esc(memberCount)}只</span>` : "",
          leaderRank ? `<span>领涨#${esc(leaderRank)}</span>` : "",
          coreRank ? `<span>中军#${esc(coreRank)}</span>` : "",
          change ? `<span>涨幅 ${esc(change)}</span>` : ""
        ].filter(Boolean).join("");
        const relation = [
          leader ? `<span>领涨 ${esc(leader)}</span>` : "",
          core ? `<span>中军 ${esc(core)}</span>` : ""
        ].filter(Boolean).join("");
        return `<article class="theme-role-card">
          <div class="theme-role-head">
            <div class="theme-role-title" title="${esc(item.theme_name)}">${esc(item.theme_name)}</div>
            <span class="theme-role-badge${themeRoleBadgeClass(strength)}">${esc(strength)}</span>
          </div>
          ${meta ? `<div class="theme-role-row">${meta}</div>` : ""}
          ${relation ? `<div class="theme-role-row">${relation}</div>` : ""}
          ${reason ? `<div class="theme-role-reason">${esc(reason)}</div>` : ""}
        </article>`;
      }).join("")}</div>`;
    }
    function themeEvidenceName(item) {
      return clean(item?.theme_name || item?.anchor || item?.concept_name || "");
    }
    function themeEvidenceRows(sections) {
      const influencePart = sections.find(part => part.label === "带动性" || part.type === "influence");
      const rolePart = sections.find(part => part.label === "头条题材角色" || part.type === "headline_theme_roles");
      const map = new Map();
      const ensure = name => {
        const key = clean(name);
        if (!key) return null;
        if (!map.has(key)) map.set(key, { key, name: key, influenceItem: null, influencePayload: {}, roleItem: null });
        return map.get(key);
      };
      const influences = Array.isArray(influencePart?.payload) ? influencePart.payload : [];
      for (const item of influences) {
        if (!item || typeof item !== "object") continue;
        const entry = ensure(themeEvidenceName(item));
        if (!entry) continue;
        entry.influenceItem = item;
        entry.influencePayload = parseJsonObject(item.payload || item.influence_payload || {});
      }
      const roles = Array.isArray(rolePart?.payload) ? rolePart.payload : [];
      for (const item of roles) {
        if (!item || typeof item !== "object") continue;
        const entry = ensure(themeEvidenceName(item));
        if (!entry) continue;
        entry.roleItem = item;
      }
      return Array.from(map.values())
        .filter(item => item.influenceItem || item.roleItem)
        .sort((a, b) => {
          const ai = a.influenceItem || {};
          const bi = b.influenceItem || {};
          const ar = a.roleItem || {};
          const br = b.roleItem || {};
          const selected = Number(Boolean(bi.selected)) - Number(Boolean(ai.selected));
          if (selected) return selected;
          return Number(bi.influence || 0) - Number(ai.influence || 0)
            || Number(ai.theme_rank || ar.theme_rank || 999) - Number(bi.theme_rank || br.theme_rank || 999)
            || Number(br.match_score || 0) - Number(ar.match_score || 0);
        });
    }
    function themeMetricHtml(label, value) {
      const text = clean(value);
      if (!text) return "";
      return `<div class="theme-detail-metric"><span>${esc(label)}</span><strong title="${esc(text)}">${esc(text)}</strong></div>`;
    }
    function themeRelationHtml(label, value) {
      const text = clean(value);
      return text ? `<span>${esc(label)} ${esc(text)}</span>` : "";
    }
    function mergedThemeEvidenceHtml(row, sections) {
      const rows = themeEvidenceRows(sections);
      if (!rows.length) return "";
      const detailKey = rowKey(row);
      const activeName = activeDetailTheme.get(detailKey);
      const active = rows.find(item => item.key === activeName) || rows[0];
      const tabs = rows.map(item => {
        const influence = item.influenceItem || {};
        const score = Number(influence.influence || 0);
        return `<button class="theme-switch-card ${item === active ? "active" : ""}" type="button" data-theme-tab="${esc(detailKey)}" data-theme-name="${esc(item.key)}">
          <span class="theme-switch-name" title="${esc(item.name)}">${esc(item.name)}</span>
          ${score ? `<span class="theme-switch-score">${esc(score.toFixed(0))}分</span>` : ""}
        </button>`;
      }).join("");
      const panels = rows.map(item => {
        const influence = item.influenceItem || {};
        const payload = item.influencePayload || {};
        const role = item.roleItem || {};
        const spread = payload.spread && typeof payload.spread === "object" ? payload.spread : {};
        const position = payload.position && typeof payload.position === "object" ? payload.position : {};
        const first = payload.first && typeof payload.first === "object" ? payload.first : {};
        const label = clean(influence.influence_label || payload.label || "观察");
        const tone = influenceToneClass(label, influence.influence);
        const waveNo = Number(influence.wave_no || position.wave_no || 1);
        const trigger = Number(influence.wave_trigger_order || position.trigger_order || 0);
        const firstText = [clean(first.name), clean(first.code), clean(first.time)].filter(Boolean).join(" ");
        const preheat = clean(payload.preheat?.label || "");
        const delay = clean(position.delay_text || "");
        const roleLabel = clean(role.role_label || "");
        const strength = clean(role.explain_strength || "");
        const changeNumber = Number(role.pct_change);
        const change = Number.isFinite(changeNumber) && Math.abs(changeNumber) >= 0.01 ? `${changeNumber > 0 ? "+" : ""}${changeNumber.toFixed(2)}%` : "";
        const leader = themeRoleStock(role.leader_name, role.leader_code);
        const core = themeRoleStock(role.core_name, role.core_code);
        const relation = [
          roleLabel ? themeRelationHtml("角色", roleLabel) : "",
          strength ? themeRelationHtml("解释", strength) : "",
          Number(role.member_count || 0) ? themeRelationHtml("成员", `${Number(role.member_count)}只`) : "",
          leader ? themeRelationHtml("领涨", `${leader}${Number(role.rank_leader || 0) ? ` #${Number(role.rank_leader)}` : ""}`) : "",
          core ? themeRelationHtml("中军", `${core}${Number(role.rank_core || 0) ? ` #${Number(role.rank_core)}` : ""}`) : "",
          change ? themeRelationHtml("涨幅", change) : "",
          Number(role.match_score || 0) ? themeRelationHtml("匹配", `${Number(role.match_score).toFixed(0)}分`) : ""
        ].filter(Boolean).join("");
        const metrics = [
          themeMetricHtml("带动判断", label),
          themeMetricHtml("触发位置", trigger ? (waveNo > 1 ? `第${waveNo}波 #${trigger}` : `#${trigger}`) : ""),
          themeMetricHtml("扩散强度", `3m+${Number(influence.new_after_3m ?? spread.m3 ?? 0)} / 5m+${Number(influence.new_after_5m ?? spread.m5 ?? 0)} / 10m+${Number(influence.new_after_10m ?? spread.m10 ?? 0)}`),
          themeMetricHtml("首发", firstText),
          themeMetricHtml("启动温度", preheat),
          themeMetricHtml("相对首发", delay && trigger > 1 ? delay : "")
        ].filter(Boolean).join("");
        const timeNodes = structuredSequenceNodes(payload.time_sequence, false);
        const activeNodes = structuredSequenceNodes(payload.active_sequence, true);
        const reason = clean(role.reason_explain || role.concept_name || "");
        return `<article class="theme-detail-panel" data-theme-panel="${esc(detailKey)}" data-theme-name="${esc(item.key)}" ${item === active ? "" : "hidden"}>
          <div class="theme-detail-title">
            <div class="theme-detail-name" title="${esc(item.name)}">${esc(item.name)}</div>
            <span class="evidence-chip ${esc(tone)}">${esc(label)}</span>
          </div>
          ${metrics ? `<div class="theme-detail-grid">${metrics}</div>` : ""}
          ${relation ? `<div class="theme-detail-section"><div class="theme-detail-label">头条题材角色</div><div class="theme-relation-row">${relation}</div></div>` : ""}
          ${reason ? `<div class="theme-detail-section"><div class="theme-detail-label">概念解释</div><div class="theme-explain">${esc(reason)}</div></div>` : ""}
          ${sequenceFlowFromNodesHtml("时间序列", timeNodes)}
          ${sequenceFlowFromNodesHtml("活跃序列", activeNodes)}
        </article>`;
      }).join("");
      const body = `<div class="theme-switch-board">
        <div class="theme-switch-tabs">${tabs}</div>
        ${panels}
      </div>`;
      return `<section class="detail-card impact">
        <div class="detail-card-head">
          <div class="detail-card-title">头条题材证据</div>
        </div>
        <div class="detail-card-body">${body}</div>
      </section>`;
    }
    function evidenceBodyHtml(part) {
      if (part.type === "headline_theme_roles" || part.label === "头条题材角色") {
        const html = themeRoleCardsHtml(part);
        if (html) return html;
      }
      if (part.label === "带动性") {
        const structured = Array.isArray(part.payload) ? part.payload : [];
        if (structured.length && structured.some(item => item && typeof item === "object" && clean(item.theme_name || item.anchor))) {
          const html = multiThemeInfluenceHtml(structured);
          if (html) return html;
        }
        const structuredHtml = structuredInfluenceHtml(part.structured_payload || (!Array.isArray(part.payload) ? part.payload : null), structured);
        if (structuredHtml) return `<div class="impact-list compact">${structuredHtml}</div>`;
        const lines = structured.length
          ? structured.slice(0, 8).map(item => clampText(typeof item === "string" ? item : item.reason || item.evidence || "", 150)).filter(Boolean)
          : String(part.body || "").split(/\n+/).map(clean).filter(Boolean);
        return `<div class="impact-list compact">${lines.map(influenceLineHtml).filter(Boolean).join("")}</div>`;
      }
      if (part.label === "有效事实总结" || part.label === "事实时间线" || part.label === "关键事实" || part.label === "区间领头" || part.label === "带动性" || part.label === "龙虎榜席位" || part.label === "证据缺口") {
        const structured = Array.isArray(part.payload) ? part.payload : [];
        const maxLines = part.label === "带动性" ? 8 : 3;
        const displayLines = Array.isArray(part.display_lines) ? part.display_lines.map(clean).filter(Boolean) : [];
        const lines = displayLines.length
          ? displayLines.slice(0, maxLines)
          : structured.length
          ? structured.slice(0, maxLines).map(item => clampText(typeof item === "string" ? item : item.reason || item.evidence || "", part.label === "带动性" ? 140 : 120)).filter(Boolean)
          : String(part.body || "").split(/\n+/).map(clean).filter(Boolean);
        return `<div class="impact-list">${lines.map(line => `<div class="impact-item"><div class="impact-evidence">${esc(line)}</div></div>`).join("")}</div>`;
      }
      if (part.label === "核心证据") {
        const structured = Array.isArray(part.payload) ? part.payload : [];
        const lines = structured.length
          ? structured.slice(0, 3).map(item => {
              const title = clampText(item.title || item.reason || "", 72);
              const reason = clampText(item.reason || "", 95);
              const when = clean(item.source_date || item.timeliness || "");
              return `${when ? `${when} ` : ""}${title}${reason && reason !== title ? `：${reason}` : ""}`;
            })
          : String(part.body || "").split(/\n+/).map(clean).filter(Boolean);
        return `<div class="impact-list">${lines.map(line => `<div class="impact-item"><div class="impact-evidence">${esc(line)}</div></div>`).join("")}</div>`;
      }
      if (part.label === "影响要素") {
        const structured = Array.isArray(part.payload) ? part.payload : [];
        const lines = structured.length
          ? structured.slice(0, 3).map(item => {
              const factor = clean(item.factor_type || "影响");
              const direction = clean(item.direction || "");
              const importance = clean(item.importance || "");
              const evidence = clampText(item.evidence || "", 130);
              return `${factor}${direction ? `/${direction}` : ""}${importance ? `/${importance}` : ""}：${evidence}`;
            })
          : String(part.body || "").split(/\n+/).map(clean).filter(Boolean);
        return `<div class="impact-list">${lines.map(impactLineHtml).join("")}</div>`;
      }
      return `<div class="evidence-text">${escBlock(part.body)}</div>`;
    }
    function evidenceSectionClass(part) {
      if (part.class_name) return part.class_name;
      if (part.className) return part.className;
      return evidenceMeta(part).className;
    }
    function sourceCycleLabel(part) {
      const registry = parseJsonObject(part.source_registry);
      if (!Object.keys(registry).length) return "";
      if (registry.mutable_intraday) return "盘中可变";
      const cycle = clean(registry.update_cycle || "");
      const labels = {
        after_close_daily: "盘后日更",
        dirty_or_manual: "变更刷新",
        dirty_or_minute: "分钟/变更",
        daily_or_manual: "日更/手动",
        manual_or_batch: "批量维护",
        on_demand: "按需更新",
        async_task: "异步任务",
        scan_loop: "扫描实时",
        window_loop: "窗口实时"
      };
      return labels[cycle] || "";
    }
    function sourceCycleTitle(part) {
      const registry = parseJsonObject(part.source_registry);
      if (!Object.keys(registry).length) return "";
      return [
        clean(registry.source_table || ""),
        clean(registry.source_generation || ""),
        clean(registry.update_cycle || ""),
        clean(registry.data_date_policy || "")
      ].filter(Boolean).join(" · ");
    }
    function evidenceParts(row) {
      const structured = parseJsonArray(row.evidence_items)
        .map(normalizeEvidenceItem)
        .filter(Boolean);
      if (structured.length) {
        return structured.filter((part, index, arr) =>
          !arr.some((prev, prevIndex) => prevIndex < index && prev.layer === part.layer && prev.label === part.label && prev.body === part.body)
        );
      }
      const raw = normalizeEvidenceRaw(row);
      if (!raw) return [];
      const parts = [];
      const pattern = /(【实时证据】|【异步证据】)|(异步总结|有效事实总结|事实时间线|影响要素|题材证据|个股证据|事件|公告|题材)：/g;
      const matches = [...raw.matchAll(pattern)];
      let currentLayer = "实时证据";
      for (let i = 0; i < matches.length; i += 1) {
        if (matches[i][1]) {
          currentLayer = matches[i][1].replace(/[【】]/g, "");
          continue;
        }
        const label = matches[i][2];
        const start = matches[i].index || 0;
        const bodyStart = start + matches[i][0].length;
        let end = raw.length;
        for (let j = i + 1; j < matches.length; j += 1) {
          if (matches[j][1] || matches[j][2]) {
            end = matches[j].index || raw.length;
            break;
          }
        }
        let body = clean(raw.slice(bodyStart, end));
        body = body.replace(/【实时证据】|【异步证据】/g, "").trim();
        if (body && !parts.some(part => part.layer === currentLayer && part.label === label && part.body === body)) {
          parts.push({ layer: currentLayer, label, body });
        }
      }
      return parts;
    }
    function sectionCollapsed(part) {
      if (part.collapsed) return true;
      if (part.layer !== "异步证据") return false;
      return ["核心支撑", "核心证据", "影响要素", "锚点一致性", "异动质量", "时效判断", "核心结论"].includes(part.label);
    }
    function evidenceSectionHtml(part) {
      const title = part.label === "题材" ? "题材要点" : part.label;
      const dataDate = part.availability === "intraday" ? "" : clean(part.data_date || part.evidence_date || "");
      const relation = clean(part.data_relation_label || "");
      const dateChip = dataDate ? `<span class="evidence-date-chip">${esc(relation && relation !== "待核数据日" ? `${relation} ${dataDate}` : `数据日 ${dataDate}`)}</span>` : "";
      const cycleLabel = sourceCycleLabel(part);
      const cycleTitle = sourceCycleTitle(part);
      const cycleChip = cycleLabel ? `<span class="evidence-cycle-chip" title="${esc(cycleTitle)}">${esc(cycleLabel)}</span>` : "";
      const head = `<div class="evidence-heading">
        <span>${esc(title)}</span>
        <span class="evidence-source-wrap">
          ${dateChip}
          ${cycleChip}
          <span class="evidence-source">${esc(part.source || evidenceSource(part.label, part.body))}</span>
        </span>
      </div>`;
      const body = evidenceBodyHtml(part);
      if (sectionCollapsed(part)) {
        return `<details class="evidence-fold ${evidenceSectionClass(part)}">
          <summary>${head}</summary>
          <div class="evidence-fold-body">${body}</div>
        </details>`;
      }
      return `<section class="evidence-section ${evidenceSectionClass(part)}">
        ${head}
        ${body}
      </section>`;
    }
    function evidenceLayerTitle(layer) {
      return layer === "异步证据" ? EVIDENCE_LAYER_META.async.title : EVIDENCE_LAYER_META.realtime.title;
    }
    function evidenceLayerHint(layer) {
      return layer === "异步证据" ? EVIDENCE_LAYER_META.async.hint : EVIDENCE_LAYER_META.realtime.hint;
    }
    function evidenceHtml(row) {
      const view = evidenceView(row);
      const viewLayers = Array.isArray(view.layers) ? view.layers : [];
      const renderedLayers = viewLayers
        .map(group => ({
          layer: clean(group.layer || ""),
          title: clean(group.title || ""),
          hint: clean(group.hint || ""),
          className: clean(group.class_name || group.className || group.layer || ""),
          sections: Array.isArray(group.sections) ? group.sections : []
        }))
        .filter(group => group.sections.length);
      if (renderedLayers.length) {
        return `<div class="evidence-block">${renderedLayers.map(group => `<section class="evidence-layer ${esc(group.className)}">
          <div class="evidence-layer-title">
            <span>${esc(group.title || (group.layer === "async" || group.layer === "异步证据" ? EVIDENCE_LAYER_META.async.title : EVIDENCE_LAYER_META.realtime.title))}</span>
            <small>${esc(group.hint || "")}</small>
          </div>
          ${group.sections.map(evidenceSectionHtml).join("")}
        </section>`).join("")}</div>`;
      }
      const parts = evidenceParts(row);
      if (!parts.length) return `<div class="detail-empty">暂无证据详情</div>`;
      const layers = ["实时证据", "异步证据"].map(layer => ({
        layer,
        parts: curateEvidenceParts(parts, layer)
      })).filter(group => group.parts.length);
      return `<div class="evidence-block">${layers.map(group => `<section class="evidence-layer ${group.layer === "异步证据" ? "async" : "realtime"}">
        <div class="evidence-layer-title">
          <span>${esc(evidenceLayerTitle(group.layer))}</span>
          <small>${esc(evidenceLayerHint(group.layer))}</small>
        </div>
        ${group.parts.map(evidenceSectionHtml).join("")}
      </section>`).join("")}</div>`;
    }
    function evidenceLevelInfo(row) {
      const tags = parseTags(row.tags).map(clean).filter(Boolean).map(tagText);
      const explicit = tags.find(tag => tag.startsWith("证据:"));
      const value = explicit ? explicit.slice("证据:".length) : "";
      const raw = String(row.detail ?? "");
      const parts = evidenceParts(row);
      const hasAnnouncement = /公告|年报|半年报|互动易|问询|回复/.test(raw);
      const hasDirectEvidence = parts.some(part => ["题材证据", "个股证据"].includes(part.label));
      const hasImpact = parts.some(part => part.label === "影响要素");
      const hasRealtime = parts.some(part => part.layer === "实时证据");
      const hasAsync = parts.some(part => part.layer === "异步证据");
      if (value && value !== "pending") return { level: value, cls: value.includes("强") ? "strong" : "", basis: `${hasRealtime ? "实时链路" : ""}${hasRealtime && hasAsync ? " + " : ""}${hasAsync ? "有效事实" : ""} · 来自证据层标记` };
      if (hasImpact) return { level: "强", cls: "strong", basis: `${hasRealtime ? "实时链路" : ""}${hasRealtime && hasAsync ? " + " : ""}${hasAsync ? "有效事实" : ""} · 已提取影响股价要素` };
      if (hasAnnouncement && hasDirectEvidence) return { level: "强", cls: "strong", basis: `${hasRealtime ? "实时链路" : ""}${hasRealtime && hasAsync ? " + " : ""}${hasAsync ? "有效事实" : ""} · 公司披露/互动易 + 题材解释` };
      if (hasDirectEvidence || parts.length) return { level: "中", cls: "", basis: `${hasRealtime ? "实时链路" : ""}${hasRealtime && hasAsync ? " + " : ""}${hasAsync ? "有效事实" : ""} · 题材/个股解释` };
      return { level: "待补全", cls: "weak", basis: "暂无可核验证据" };
    }
    function cleanJudgementText(text) {
      return clean(text)
        .replace(/^最强解释是/, "")
        .replace(/^异动主要由/, "")
        .replace(/^异动主要因/, "")
        .replace(/^因/, "")
        .replace(/。$/, "");
    }
    function evidenceFactText(item) {
      if (!item || typeof item !== "object") return "";
      const reason = clean(item.reason || item.evidence || "");
      const title = clean(item.title || item.factor_type || "");
      const date = clean(item.source_date || "");
      let text = reason || title;
      if (!text) return "";
      if (title && reason && !reason.includes(title) && title.length <= 18 && !/公告|报告|披露|资料|龙虎榜/.test(reason)) {
        text = `${title}：${reason}`;
      }
      return clampText(`${date ? `${date} ` : ""}${text}`, 92);
    }
    function evidenceSummaryFacts(parts) {
      const facts = [];
      const addFact = value => {
        const text = clampText(clean(value), 92);
        if (!text) return;
        if (/逻辑|较硬|推论|判断|解释强度/.test(text) && !/\d|同比|净买|订单|合同|客户|供货|中标|业绩|净利|营收|龙虎榜/.test(text)) return;
        if (!facts.some(item => item === text || item.includes(text) || text.includes(item))) facts.push(text);
      };
      const factPart = parts.find(part => part.label === "有效事实总结") || parts.find(part => part.label === "事实时间线") || parts.find(part => part.label === "关键事实");
      const factPayload = Array.isArray(factPart?.payload) ? factPart.payload : [];
      factPayload.slice(0, 3).forEach(addFact);
      if (factPart?.body) {
        String(factPart.body).split(/\n+/).slice(0, 3).forEach(addFact);
      }
      const periodPart = parts.find(part => part.label === "区间领头");
      if (periodPart?.body) {
        String(periodPart.body).split(/\n+/).slice(0, 2).forEach(addFact);
      }
      const corePart = parts.find(part => part.label === "核心证据");
      const corePayload = Array.isArray(corePart?.payload) ? corePart.payload : [];
      corePayload.slice(0, 5).forEach(item => addFact(evidenceFactText(item)));
      const impactPart = parts.find(part => part.label === "影响要素");
      const impactPayload = Array.isArray(impactPart?.payload) ? impactPart.payload : [];
      impactPayload.slice(0, 5).forEach(item => addFact(evidenceFactText(item)));
      const supportPart = parts.find(part => part.label === "核心支撑");
      const supportPayload = Array.isArray(supportPart?.payload) ? supportPart.payload : [];
      supportPayload.slice(0, 3).forEach(item => addFact(typeof item === "string" ? item : evidenceFactText(item)));
      if (!facts.length && supportPart?.body) {
        String(supportPart.body).split(/\n+/).slice(0, 2).forEach(addFact);
      }
      if (!facts.length && impactPart?.body) {
        String(impactPart.body).split(/\n+/).slice(0, 2).forEach(line => {
          const match = clean(line).match(/：(.+)$/);
          addFact(match ? match[1] : line);
        });
      }
      return facts.slice(0, 3);
    }
    function lineWithPrefix(part, prefix) {
      if (!part) return "";
      const payloadLines = Array.isArray(part.payload)
        ? part.payload.map(item => clean(typeof item === "string" ? item : item?.reason || item?.evidence || "")).filter(Boolean)
        : [];
      const bodyLines = String(part.body || "").split(/\n+/).map(clean).filter(Boolean);
      return [...payloadLines, ...bodyLines].find(line => line.startsWith(prefix)) || "";
    }
    function stripPrefix(line) {
      const idx = clean(line).indexOf("：");
      return idx >= 0 ? clean(line).slice(idx + 1) : clean(line);
    }
    function decisionCard(label, value, cls = "") {
      if (!value) return "";
      return `<div class="decision-card ${esc(cls)}">
        <span class="decision-label">${esc(label)}</span>
        <span class="decision-value" title="${esc(value)}">${esc(value)}</span>
      </div>`;
    }
    function displayContract(row) {
      const contract = parseJsonObject(row.display_contract);
      return Number(contract.schema_version) >= 1 ? contract : {};
    }
    function contractDecisionCards(contract) {
      const cards = Array.isArray(contract.decision_cards) ? contract.decision_cards : [];
      return cards.slice(0, 4)
        .map(card => decisionCard(clean(card.label), clean(card.value), clean(card.tone || "")))
        .join("");
    }
    function contractChips(contract) {
      const chips = Array.isArray(contract.chips) ? contract.chips : [];
      return chips.slice(0, 5).map(chip => {
        const label = clean(chip.label);
        const value = clean(chip.value);
        if (!label || !value) return "";
        return `<span class="evidence-chip ${esc(clean(chip.tone || "weak"))}">${esc(label)} ${esc(value)}</span>`;
      }).join("");
    }
    function allEvidenceSections(row) {
      const view = evidenceView(row);
      const layers = Array.isArray(view.layers) ? view.layers : [];
      return layers.flatMap(layer =>
        (Array.isArray(layer.sections) ? layer.sections : []).map(section => ({
          ...section,
          groupTitle: clean(layer.title || ""),
          groupAvailability: clean(layer.availability || layer.layer || "")
        }))
      );
    }
    function sectionLines(part, max = 3, limit = 118) {
      if (!part) return [];
      const payload = Array.isArray(part.payload) ? part.payload : [];
      const fromPayload = payload.map(item => clean(typeof item === "string" ? item : item?.reason || item?.evidence || item?.title || "")).filter(Boolean);
      const fromBody = String(part.body || "").split(/\n+/).map(clean).filter(Boolean);
      const lines = [...fromPayload, ...fromBody];
      const out = [];
      for (const line of lines) {
        const text = clampText(line, limit);
        if (text && !out.some(item => item === text || item.includes(text) || text.includes(item))) out.push(text);
        if (out.length >= max) break;
      }
      return out;
    }
    function sectionDateLabel(part) {
      if (!part || part.availability === "intraday") return "";
      const date = clean(part.data_date || part.evidence_date || "");
      const relation = clean(part.data_relation_label || "");
      if (!date) return relation && relation !== "待核数据日" ? relation : "";
      return relation && relation !== "待核数据日" ? `${relation} ${date}` : `数据日 ${date}`;
    }
    function focusGroupHtml(title, tone, meta, lines) {
      const useful = lines.map(line => clean(line)).filter(Boolean).slice(0, 3);
      if (!useful.length) return "";
      return `<div class="evidence-focus ${esc(tone)}">
        <div class="evidence-focus-title"><span>${esc(title)}</span>${meta ? `<small>${esc(meta)}</small>` : ""}</div>
        <div class="evidence-focus-lines">
          ${useful.map(line => `<div class="evidence-focus-line" title="${esc(line)}">${esc(line)}</div>`).join("")}
        </div>
      </div>`;
    }
    function hardEvidenceFocusHtml(row) {
      const sections = allEvidenceSections(row).filter(part => {
        const group = clean(part.groupAvailability || part.evidence_group || "");
        const status = clean(part.valid_status || "");
        const display = clean(part.display_level || "");
        return ["model_summary", "current_effective", "post_close_confirm"].includes(group)
          && !["expired", "historical", "invalid"].includes(status)
          && display !== "background";
      });
      if (!sections.length) return "";
      const byLabel = label => sections.find(part => part.label === label);
      const hardParts = sections.filter(part => {
        const type = clean(part.type || "");
        const label = clean(part.label || "");
        return ["announcement", "core", "impact"].includes(type) || ["当前硬催化", "核心证据", "有效事实总结", "事实时间线", "影响要素"].includes(label);
      });
      const period = byLabel("区间领头");
      const hardPattern = /净利|营收|同比|订单|合同|中标|客户|供货|公告|互动易|财报|季报|年报|龙虎榜|机构|股通|游资|锚点内/;
      const hardLines = hardParts.flatMap(part => sectionLines(part, 2))
        .filter(line => hardPattern.test(line))
        .slice(0, 3);
      const rankLines = sectionLines(period, 3, 108);
      const html = [
        focusGroupHtml("硬催化", "good", sectionDateLabel(hardParts[0]), hardLines),
        focusGroupHtml("强度位置", "rank", sectionDateLabel(period), rankLines)
      ].join("");
      return html ? `<div class="evidence-focus-grid">${html}</div>` : "";
    }
    function evidenceFlowHtml(row) {
      const view = evidenceView(row);
      const layers = Array.isArray(view.layers) ? view.layers : [];
      const sourceTime = clean(view.latest_source_updated_at || row.latest_source_updated_at || "");
      const sourceChip = sourceTime ? `<span class="evidence-flow-chip">来源更新 ${esc(timeText(sourceTime))}</span>` : "";
      const chips = layers
        .map(layer => {
          const title = clean(layer.title || layer.availability_label || "");
          const count = Array.isArray(layer.sections) ? layer.sections.length : 0;
          return title && count ? `<span class="evidence-flow-chip">${esc(title)} ${count}</span>` : "";
        })
        .filter(Boolean)
        .join("") + sourceChip;
      return chips ? `<div class="evidence-flow">${chips}</div>` : "";
    }
    function evidenceSummaryHtml(row) {
      const view = evidenceView(row);
      const summary = view && typeof view.summary === "object" && !Array.isArray(view.summary) ? view.summary : null;
      if (summary) {
        const cards = Array.isArray(summary.cards) ? summary.cards : [];
        const chips = Array.isArray(summary.chips) ? summary.chips : [];
        const facts = Array.isArray(summary.facts) ? summary.facts.map(clean).filter(Boolean).slice(0, 3) : [];
        const reason = clampText(summary.reason || "", 90);
        const flaw = clampText(summary.flaw || "", 90);
        const gap = clampText(summary.gap || "", 110);
        const focus = hardEvidenceFocusHtml(row);
        const flow = evidenceFlowHtml(row);
        return `<div class="evidence-summary">
          <div class="evidence-verdict">
            <strong>${esc(clean(summary.title || (reason ? "异动原因" : facts.length ? "关键事实" : "证据强度")))}</strong>
            <span class="evidence-level ${esc(clean(summary.level_class || ""))}">${esc(clean(summary.level || "待补全"))}</span>
          </div>
          ${reason ? `<div class="evidence-judgement"><span>原因</span>${esc(reason)}</div>` : ""}
          ${focus}
          <div class="decision-grid">
            ${cards.slice(0, 4).map(card => decisionCard(clean(card.label), clean(card.value), clean(card.tone || ""))).join("")}
          </div>
          <div class="evidence-chip-row">
            ${chips.slice(0, 5).map(chip => {
              const label = clean(chip.label);
              const value = clean(chip.value);
              return label && value ? `<span class="evidence-chip ${esc(clean(chip.tone || "weak"))}">${esc(label)} ${esc(value)}</span>` : "";
            }).join("")}
          </div>
          ${!focus && facts.length ? `<ul class="evidence-facts">${facts.map(fact => `<li class="evidence-fact">${esc(fact)}</li>`).join("")}</ul>` : ""}
          ${flaw ? `<div class="evidence-judgement"><span>瑕疵</span>${esc(flaw)}</div>` : ""}
          ${flow}
          <div class="evidence-basis">${esc(clean(summary.basis || ""))}</div>
          ${gap ? `<div class="evidence-gap">${esc(gap)}</div>` : ""}
        </div>`;
      }
      const info = evidenceLevelInfo(row);
      const parts = evidenceParts(row);
      const contract = displayContract(row);
      const movePart = parts.find(part => part.label === "异动解释");
      const finalPart = parts.find(part => part.label === "核心结论");
      const impact = parts.find(part => part.label === "影响要素");
      const facts = evidenceSummaryFacts(parts);
      const firstLine = part => part?.body ? clampText(String(part.body || "").split(/\n+/).map(clean).filter(Boolean)[0] || "", 96) : "";
      const judgement = movePart
        ? clampText(cleanJudgementText(movePart.body), 90)
        : finalPart
        ? clampText(cleanJudgementText(finalPart.body), 90)
        : impact ? clampText(String(impact.body || "").split(/\n+/).map(clean).filter(Boolean)[0] || "", 90) : "";
      const sustainPart = parts.find(part => part.label === "持续依据") || parts.find(part => part.label === "区间领头");
      const sustain = firstLine(sustainPart);
      const influencePart = parts.find(part => part.label === "带动性");
      const qualityPart = parts.find(part => part.label === "持续性");
      const influencePosition = stripPrefix(firstLine(influencePart));
      const preheat = stripPrefix(lineWithPrefix(influencePart, "本股启动前"));
      const firstQuality = stripPrefix(lineWithPrefix(influencePart, "首发质量"));
      const spread = stripPrefix(lineWithPrefix(influencePart, "扩散"));
      const flawPart = parts.find(part => part.label === "最大瑕疵");
      const gapPart = parts.find(part => part.label === "证据缺口");
      const flaw = flawPart ? clampText(flawPart.body, 90) : "";
      const firstGap = gapPart ? clampText(String(gapPart.body || "").split(/\n+/).map(clean).filter(Boolean)[0] || "", 90) : "";
      const gap = info.level === "待补全"
        ? "建议补充：公告、互动易、定期报告或同花顺题材解释。"
        : flaw || firstGap ? "" : impact ? "" : parts.some(part => ["公告", "事件"].includes(part.label)) ? "" : "可继续补充公告/互动易作为硬证据。";
      const contractCards = contractDecisionCards(contract);
      const contractChipHtml = contractChips(contract);
      const focus = hardEvidenceFocusHtml(row);
      const flow = evidenceFlowHtml(row);
      return `<div class="evidence-summary">
        <div class="evidence-verdict">
          <strong>${judgement ? "异动原因" : facts.length ? "关键事实" : "证据强度"}</strong>
          <span class="evidence-level ${esc(info.cls)}">${esc(info.level)}</span>
        </div>
        ${judgement ? `<div class="evidence-judgement"><span>原因</span>${esc(judgement)}</div>` : ""}
        ${focus}
        <div class="decision-grid">
          ${contractCards || `
            ${decisionCard("持续性", firstLine(qualityPart) || info.level)}
            ${decisionCard("带动性", influencePosition)}
            ${decisionCard("启动前", preheat)}
            ${decisionCard("首发", firstQuality)}
          `}
        </div>
        <div class="evidence-chip-row">
          ${contractChipHtml || `
            ${spread ? `<span class="evidence-chip ${/强扩散/.test(spread) ? "good" : /弱扩散/.test(spread) ? "warn" : "weak"}">扩散 ${esc(spread)}</span>` : ""}
            ${sustain ? `<span class="evidence-chip good">持续 ${esc(sustain)}</span>` : ""}
          `}
        </div>
        ${!focus && facts.length ? `<ul class="evidence-facts">${facts.map(fact => `<li class="evidence-fact">${esc(fact)}</li>`).join("")}</ul>` : ""}
        ${flaw ? `<div class="evidence-judgement"><span>瑕疵</span>${esc(flaw)}</div>` : firstGap ? `<div class="evidence-judgement"><span>缺口</span>${esc(firstGap)}</div>` : ""}
        ${flow}
        <div class="evidence-basis">${esc(info.basis)}</div>
        ${gap ? `<div class="evidence-gap">${esc(gap)}</div>` : ""}
      </div>`;
    }
    function detailLevelClass(level, cls = "") {
      const text = clean(level);
      if (cls) return cls;
      if (/强|可持续|高/.test(text)) return "strong";
      if (/弱|缺|走弱|待/.test(text)) return "warn";
      return "";
    }
    function detailReasonText(row, sections) {
      const view = evidenceView(row);
      const summary = view && typeof view.summary === "object" && !Array.isArray(view.summary) ? view.summary : {};
      const move = sections.find(part => part.label === "异动解释");
      const impact = sections.find(part => part.label === "影响要素");
      const rawSummary = clean(row.summary || "");
      const withoutHighlight = rawSummary.replace(/^【亮点】[^；]*；?/, "");
      return clampText(
        clean(summary.reason || move?.body || impact?.body || withoutHighlight || row.detail || "暂无清晰异动解释"),
        150
      );
    }
    function detailVisibleSections(row) {
      const wanted = new Set([
        "带动性",
        "头条题材角色",
        "有效事实总结",
        "事实时间线",
        "关键事实",
        "核心证据",
        "影响要素",
        "个股证据",
        "最大瑕疵",
        "证据缺口"
      ]);
      const hiddenLabels = new Set(["领涨中军", "题材背景", "龙虎榜席位", "区间领头", "核心支撑", "持续依据", "锚点一致性", "时效判断", "异动质量", "核心结论", "异动解释", "持续性", "题材证据"]);
      const order = {
        "带动性": 0,
        "头条题材角色": 2,
        "有效事实总结": 6,
        "事实时间线": 6,
        "关键事实": 6,
        "核心证据": 7,
        "影响要素": 8,
        "题材证据": 9,
        "个股证据": 10,
        "最大瑕疵": 20,
        "证据缺口": 21
      };
      const seen = new Set();
      return allEvidenceSections(row)
        .filter(part => {
          const group = clean(part.groupAvailability || part.evidence_group || "");
          const status = clean(part.valid_status || "");
          const label = clean(part.label || "");
          const type = clean(part.type || "");
          if (!["model_summary", "current_effective", "post_close_confirm"].includes(group)) return false;
          if (["expired", "historical", "invalid"].includes(status)) return false;
          if (hiddenLabels.has(label)) return false;
          if (!wanted.has(label) && !["influence", "headline_theme_roles", "facts", "core", "impact"].includes(type)) return false;
          return true;
        })
        .sort((a, b) => (order[a.label] ?? 100) - (order[b.label] ?? 100) || evidencePartPriority(a) - evidencePartPriority(b))
        .filter(part => {
          const key = `${clean(part.label)}|${clean(part.body).slice(0, 80)}`;
          if (seen.has(key)) return false;
          seen.add(key);
          return true;
        })
        .slice(0, 8);
    }
    function detailCardHtml(part) {
      const body = evidenceBodyHtml(part);
      if (!body) return "";
      const source = sourceCycleLabel(part) || clean(part.source || evidenceSource(part.label, part.body));
      return `<section class="detail-card ${evidenceSectionClass(part)}">
        <div class="detail-card-head">
          <div class="detail-card-title">${esc(part.label === "题材" ? "题材要点" : part.label)}</div>
          ${source ? `<div class="detail-card-source">${esc(source)}</div>` : ""}
        </div>
        <div class="detail-card-body">${body}</div>
      </section>`;
    }
    function detailEvidenceHtml(row) {
      const sections = detailVisibleSections(row);
      const themeCard = mergedThemeEvidenceHtml(row, sections);
      const isThemeSection = part => ["带动性", "头条题材角色"].includes(clean(part.label || "")) || ["influence", "headline_theme_roles"].includes(clean(part.type || ""));
      const isFactSummary = part => clean(part.label || "") === "有效事实总结";
      const cards = [
        ...sections
          .filter(isFactSummary)
          .map(detailCardHtml),
        themeCard,
        ...sections
          .filter(part => !isFactSummary(part) && !isThemeSection(part))
          .map(detailCardHtml)
      ].filter(Boolean).join("");
      return `<div class="detail-shell">
        ${cards ? `<div class="detail-card-grid">${cards}</div>` : `<div class="detail-empty">暂无有效证据详情</div>`}
      </div>`;
    }
    function pctText(value) {
      const number = Number(value);
      if (!Number.isFinite(number)) return "";
      return `${number > 0 ? "+" : ""}${number.toFixed(2)}%`;
    }
    function metricChipsHtml(row) {
      const change = pctText(row.change_pct);
      const speed = pctText(row.speed_pct);
      const isAuction = row.kind === "auction";
      const changeLabel = row.kind === "window" ? "高" : (isAuction ? "竞" : "现");
      const tags = parseTags(row.tags).map(tagText);
      const appearance = tags.find(tag => tag.startsWith("出现:"));
      const seal = tags.find(tag => tag.startsWith("封单:"));
      const windowRank = row.kind === "window" && row.sort_rank ? `Top${row.sort_rank}` : "";
      const auctionRank = isAuction && row.sort_rank ? `Top${row.sort_rank}` : "";
      return [
        auctionRank ? `<span class="metric-chip">${esc(auctionRank)}</span>` : "",
        isAuction && seal ? `<span class="metric-chip change">${esc(seal.replace(":", " "))}</span>` : "",
        windowRank ? `<span class="metric-chip">${esc(windowRank)}</span>` : "",
        row.kind === "window" && appearance ? `<span class="metric-chip">${esc(appearance.replace(":", " "))}</span>` : "",
        change ? `<span class="metric-chip change">${esc(changeLabel)} ${esc(change)}</span>` : "",
        !isAuction && speed ? `<span class="metric-chip speed">速 ${esc(speed)}</span>` : ""
      ].filter(Boolean).join("");
    }
    function highlightText(row) {
      const summary = clean(row.summary || "");
      if (!summary.startsWith("【亮点】")) return "";
      return clean(summary.slice("【亮点】".length).split("；")[0]);
    }
    function renderDetail(row) {
      if (!row) {
        $("detailTime").textContent = "";
        $("detailPanel").innerHTML = `<div class="detail-empty">暂无详情</div>`;
        return;
      }
      $("detailTime").textContent = `${stockTitleText(row)} · ${timeText(row.event_time)}`;
      $("detailPanel").innerHTML = `<div class="detail-body">
        ${detailEvidenceHtml(row)}
      </div>`;
    }
    function streamItemHtml(row) {
      const key = rowKey(row);
      const active = key === selectedKey ? " active" : "";
      const stableRank = clean(row.kind) === "window" ? " stable-rank-item" : "";
      const highlight = highlightText(row);
      const visibleTags = visibleTagsFor(row);
      return `<article class="stream-item${stableRank}${active}" data-select-key="${esc(key)}">
        <div class="stream-title">${stockAnchorHtml(row)}</div>
        <div class="stream-metric">${metricChipsHtml(row)}</div>
        <div class="stream-tags">${visibleTags.map(tagHtml).join("")}</div>
        ${highlight ? `<div class="stream-highlight">${esc(highlight)}</div>` : `<div class="stream-highlight">暂无公司亮点</div>`}
      </article>`;
    }
    function groupKey(row) {
      return `${clean(row.event_time)}|${clean(row.kind_label || row.kind)}`;
    }
    function groupTitleHtml(row) {
      const rows = arguments.length > 1 ? arguments[1] : [row];
      const anchors = [];
      for (const item of rows) {
        for (const theme of headlineThemes(item)) {
          if (!anchors.includes(theme)) anchors.push(theme);
        }
      }
      const anchorSummary = anchors.length ? `头条题材 ${anchors.slice(0, 3).join(" / ")}` : "未命中头条题材";
      const isWindow = clean(row.kind) === "window";
      const isAuction = clean(row.kind) === "auction";
      const summary = isWindow ? `开盘至今累计强度 Top5 · 动态更新` : (isAuction ? `涨停封单 Top3 · 09:15-09:25` : `${rows.length}只 · ${anchorSummary}`);
      return `<div class="stream-group-title">
        <span>${esc(timeText(row.event_time))}</span>
        <span class="kind ${esc(row.kind)}">${esc(row.kind_label || row.kind)}</span>
        <span class="group-summary">${esc(summary)}</span>
      </div>`;
    }
    function groupedFeedHtml(rows) {
      const groups = [];
      for (const row of rows) {
        const key = groupKey(row);
        let group = groups[groups.length - 1];
        if (!group || group.key !== key) {
          group = { key, first: row, rows: [] };
          groups.push(group);
        }
        group.rows.push(row);
      }
      return groups.map(group => `<section class="stream-group ${clean(group.first.kind) === "window" ? "stable-rank-group" : ""}">
        ${groupTitleHtml(group.first, group.rows)}
        ${group.rows.map(streamItemHtml).join("")}
      </section>`).join("");
    }
    function renderList(id, rows, emptyText) {
      if (rows.error) {
        $(id).innerHTML = `<div class="error">${esc(rows.error)}</div>`;
      } else if (!rows.length) {
        $(id).innerHTML = `<div class="empty">${esc(emptyText)}</div>`;
      } else {
        $(id).innerHTML = groupedFeedHtml(rows);
      }
    }
    let availableDates = [];
    let latestTradeDate = "";
    function queryTradeDate() {
      const params = new URLSearchParams(window.location.search);
      return clean(params.get("trade_date"));
    }
    function setTradeDateParam(value) {
      const url = new URL(window.location.href);
      if (value) url.searchParams.set("trade_date", value);
      else url.searchParams.delete("trade_date");
      window.history.replaceState({}, "", url);
    }
    function feedUrl() {
      const tradeDate = clean($("tradeDateSelect").value || queryTradeDate());
      if (!tradeDate) return "/api/feed";
      return `/api/feed?trade_date=${encodeURIComponent(tradeDate)}`;
    }
    function detailUrl(row) {
      const tradeDate = clean($("tradeDateSelect").value || queryTradeDate());
      const params = new URLSearchParams();
      if (tradeDate) params.set("trade_date", tradeDate);
      params.set("kind", clean(row.kind));
      params.set("event_time", clean(row.event_time));
      params.set("code", clean(row.code));
      return `/api/feed/detail?${params.toString()}`;
    }
    function renderDetailLoading(row) {
      if (!row) {
        renderDetail(row);
        return;
      }
      $("detailTime").textContent = `${stockTitleText(row)} · ${timeText(row.event_time)}`;
      $("detailPanel").innerHTML = `<div class="detail-empty">证据详情加载中...</div>`;
    }
    function mergeDetailRow(base, detail) {
      return { ...(base || {}), ...(detail || {}), detail_loaded: true };
    }
    async function loadDetail(row) {
      if (!row) {
        renderDetail(row);
        return;
      }
      const key = rowKey(row);
      const cached = detailCache.get(key);
      if (cached && clean(cached.latest_source_updated_at) === clean(row.latest_source_updated_at)) {
        renderDetail(mergeDetailRow(row, cached));
        return;
      }
      renderDetailLoading(row);
      const requestSeq = ++detailRequestSeq;
      try {
        const response = await fetch(detailUrl(row), { cache: "no-store" });
        const data = await response.json();
        const detail = data.row || {};
        if (requestSeq !== detailRequestSeq || selectedKey !== key) return;
        detail.latest_source_updated_at = clean(detail.latest_source_updated_at || row.latest_source_updated_at);
        const merged = mergeDetailRow(row, detail);
        detailCache.set(key, detail);
        currentFeed = currentFeed.map(item => rowKey(item) === key ? merged : item);
        renderDetail(merged);
      } catch (error) {
        if (requestSeq !== detailRequestSeq || selectedKey !== key) return;
        $("detailPanel").innerHTML = `<div class="error">${esc(error)}</div>`;
      }
    }
    function renderTradeDates(selectedDate) {
      const select = $("tradeDateSelect");
      const dates = availableDates.length ? availableDates : [selectedDate].filter(Boolean);
      select.innerHTML = dates.map(day => `<option value="${esc(day)}">${esc(day)}</option>`).join("");
      if (selectedDate && dates.includes(selectedDate)) select.value = selectedDate;
      else if (latestTradeDate && dates.includes(latestTradeDate)) select.value = latestTradeDate;
    }
    async function loadTradeDates() {
      try {
        const response = await fetch("/api/trade_dates", { cache: "no-store" });
        const data = await response.json();
        availableDates = Array.isArray(data.dates) ? data.dates : [];
        latestTradeDate = clean(data.latest);
        renderTradeDates(queryTradeDate() || latestTradeDate);
      } catch {
        renderTradeDates(queryTradeDate());
      }
    }
    async function refresh() {
      try {
        const response = await fetch(feedUrl(), { cache: "no-store" });
        const data = await response.json();
        if (!queryTradeDate() && data.trade_date) {
          renderTradeDates(data.trade_date);
        }
        renderSummary(data);
        const feed = data.feed || [];
        currentFeed = Array.isArray(feed) ? feed : [];
        if (!currentFeed.some(row => rowKey(row) === selectedKey)) {
          selectedKey = rowKey(currentFeed[0] || {});
        }
        loadDetail(currentFeed.find(row => rowKey(row) === selectedKey) || currentFeed[0]);
        renderList("feed", feed, "暂无情报");
        $("refreshText").textContent = `已刷新 ${new Date().toLocaleTimeString("zh-CN", { hour12: false })}`;
      } catch (error) {
        $("feed").innerHTML = `<div class="error">${esc(error)}</div>`;
        $("refreshText").textContent = "刷新失败";
      }
    }
    $("tradeDateSelect").addEventListener("change", () => {
      setTradeDateParam($("tradeDateSelect").value);
      refresh();
    });
    $("latestBtn").addEventListener("click", () => {
      const target = latestTradeDate || availableDates[0] || "";
      renderTradeDates(target);
      setTradeDateParam(target);
      refresh();
    });
    document.addEventListener("click", event => {
      const themeTarget = event.target.closest("[data-theme-tab]");
      if (themeTarget) {
        event.preventDefault();
        event.stopPropagation();
        const key = themeTarget.dataset.themeTab || "";
        const name = themeTarget.dataset.themeName || "";
        if (key && name) activeDetailTheme.set(key, name);
        const board = themeTarget.closest(".theme-switch-board");
        if (board && name) {
          board.querySelectorAll(".theme-switch-card").forEach(node => {
            node.classList.toggle("active", node.dataset.themeName === name);
          });
          board.querySelectorAll("[data-theme-panel]").forEach(node => {
            node.hidden = node.dataset.themeName !== name;
          });
        }
        return;
      }
      const target = event.target.closest("[data-select-key]");
      if (!target) return;
      selectedKey = target.dataset.selectKey || "";
      loadDetail(currentFeed.find(row => rowKey(row) === selectedKey));
      renderList("feed", currentFeed, "暂无情报");
    });
    loadTradeDates().then(refresh);
    setInterval(refresh, 15000);
  </script>
</body>
</html>"""


MARKET_WIDTH_HTML = r"""<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>市场概览</title>
  <style>
    :root {
      color-scheme: light;
      --bg: #f6f7f9;
      --panel: #ffffff;
      --line: #e2e8f0;
      --text: #182033;
      --muted: #667085;
      --red: #d14545;
      --green: #15916f;
      --blue: #2563eb;
      --amber: #b7791f;
      --shadow: 0 12px 28px rgba(15, 23, 42, 0.06);
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      background: var(--bg);
      color: var(--text);
      font-family: "Microsoft YaHei", "PingFang SC", system-ui, -apple-system, Segoe UI, sans-serif;
      font-size: 14px;
    }
    .topbar {
      position: sticky;
      top: 0;
      z-index: 5;
      height: 58px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 14px;
      padding: 0 22px;
      background: rgba(255, 255, 255, 0.94);
      border-bottom: 1px solid var(--line);
      backdrop-filter: blur(10px);
    }
    h1 {
      margin: 0;
      font-size: 18px;
      letter-spacing: 0;
      white-space: nowrap;
    }
    .actions {
      display: flex;
      align-items: center;
      gap: 8px;
      color: var(--muted);
      font-size: 12px;
    }
    .btn, .date-input {
      height: 32px;
      border: 1px solid var(--line);
      border-radius: 7px;
      background: #fff;
      color: #334155;
      padding: 0 10px;
      font-size: 13px;
      text-decoration: none;
    }
    .btn {
      display: inline-flex;
      align-items: center;
      cursor: pointer;
      font-weight: 650;
    }
    .btn:hover {
      color: var(--blue);
      border-color: #bfdbfe;
    }
    main {
      width: min(1460px, calc(100vw - 18px));
      margin: 0 auto;
      padding: 14px 0 32px;
    }
    .service-context {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 12px;
      color: var(--muted);
      font-size: 12px;
    }
    .service-context span {
      display: inline-flex;
      align-items: center;
      min-height: 28px;
      padding: 4px 9px;
      border: 1px solid var(--line);
      border-radius: 999px;
      background: #fff;
    }
    .kpis {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 10px;
      margin-bottom: 12px;
    }
    .flow {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 10px;
      margin-bottom: 12px;
    }
    .flow-step {
      min-width: 0;
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: 10px;
      box-shadow: var(--shadow);
      padding: 11px 12px;
    }
    .flow-step span {
      display: block;
      color: var(--muted);
      font-size: 12px;
      margin-bottom: 4px;
    }
    .flow-step strong {
      display: block;
      color: #182033;
      font-size: 14px;
      line-height: 1.35;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .flow-step small {
      display: block;
      color: var(--muted);
      font-size: 12px;
      margin-top: 4px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .kpi, .panel {
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: 10px;
      box-shadow: var(--shadow);
    }
    .cycle-panel {
      margin-bottom: 12px;
      overflow: hidden;
    }
    .cycle-body {
      padding: 12px;
    }
    .cycle-summary {
      display: grid;
      grid-template-columns: 1.2fr repeat(4, minmax(0, 1fr));
      gap: 9px;
      margin-bottom: 12px;
    }
    .sh-index-strip {
      display: grid;
      grid-template-columns: repeat(5, minmax(0, 1fr));
      gap: 8px;
      margin-bottom: 12px;
    }
    .sh-index-day {
      min-width: 0;
      border: 1px solid #edf1f6;
      border-radius: 8px;
      background: #fff;
      padding: 9px 10px;
      font-variant-numeric: tabular-nums;
    }
    .sh-index-day .date {
      display: flex;
      justify-content: space-between;
      gap: 6px;
      color: #64748b;
      font-size: 12px;
      font-weight: 750;
    }
    .sh-index-day strong {
      display: block;
      margin-top: 7px;
      font-size: 20px;
      line-height: 1;
    }
    .sh-index-day small {
      display: block;
      margin-top: 6px;
      color: #64748b;
      font-size: 12px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .cycle-card {
      min-width: 0;
      border: 1px solid #edf1f6;
      border-radius: 8px;
      background: #fbfcfe;
      padding: 9px 10px;
    }
    .cycle-card span {
      display: block;
      color: var(--muted);
      font-size: 12px;
      margin-bottom: 5px;
    }
    .cycle-card strong {
      display: block;
      font-size: 18px;
      line-height: 1.2;
      font-variant-numeric: tabular-nums;
      overflow-wrap: anywhere;
    }
    .cycle-card small {
      display: block;
      margin-top: 5px;
      color: var(--muted);
      font-size: 12px;
      font-variant-numeric: tabular-nums;
    }
    .cycle-grid {
      display: grid;
      gap: 8px;
    }
    .cycle-row {
      display: grid;
      grid-template-columns: 98px minmax(180px, 1fr) minmax(180px, 1fr) 86px 86px;
      gap: 10px;
      align-items: center;
      min-height: 50px;
      padding: 9px 10px;
      border: 1px solid #edf1f6;
      border-radius: 8px;
      background: #fff;
    }
    .cycle-date {
      font-weight: 800;
      color: #243044;
      font-variant-numeric: tabular-nums;
    }
    .cycle-date small {
      color: #94a3b8;
      font-size: 11px;
      font-weight: 700;
    }
    .data-missing {
      display: inline-block;
      margin-top: 3px;
      color: #b7791f;
      font-size: 11px;
      font-weight: 700;
    }
    .power-bar {
      min-width: 0;
    }
    .power-head {
      display: flex;
      justify-content: space-between;
      gap: 8px;
      margin-bottom: 5px;
      color: #475569;
      font-size: 12px;
      font-variant-numeric: tabular-nums;
    }
    .power-head b {
      font-size: 13px;
    }
    .bar-track {
      height: 9px;
      border-radius: 999px;
      background: #eef2f7;
      overflow: hidden;
    }
    .bar-stack {
      display: flex;
      height: 100%;
      border-radius: 999px;
      overflow: hidden;
    }
    .bar-seg {
      height: 100%;
      min-width: 2px;
    }
    .seg-up { background: #f3a0a0; }
    .seg-up5 { background: #d14545; }
    .seg-limit-up { background: #8f1d1d; }
    .seg-down { background: #8ad2bf; }
    .seg-down5 { background: #15916f; }
    .seg-limit-down { background: #0b5d49; }
    .bar-fill {
      height: 100%;
      min-width: 2px;
      border-radius: 999px;
    }
    .bar-fill.red-bg { background: #ef8f8f; }
    .bar-fill.green-bg { background: #6ac5ad; }
    .cycle-count {
      text-align: right;
      color: #475569;
      font-size: 11px;
      font-weight: 700;
      font-variant-numeric: tabular-nums;
    }
    .cycle-count span {
      display: block;
      color: #64748b;
      font-size: 11px;
      line-height: 1.15;
    }
    .cycle-count b {
      display: block;
      margin-top: 4px;
      color: #182033;
      font-size: 16px;
      line-height: 1;
    }
    .cycle-count.positive b { color: #b42318; }
    .cycle-count.negative b { color: #0f766e; }
    .component-line {
      margin-top: 5px;
      color: #64748b;
      font-size: 11px;
      font-variant-numeric: tabular-nums;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .dot {
      display: inline-block;
      width: 7px;
      height: 7px;
      border-radius: 999px;
      margin-right: 3px;
      vertical-align: 1px;
    }
    .kpi {
      min-height: 124px;
      padding: 12px;
    }
    .kpi span {
      display: block;
      color: var(--muted);
      font-size: 12px;
      margin-bottom: 6px;
    }
    .kpi strong {
      display: block;
      font-size: 24px;
      line-height: 1;
      font-variant-numeric: tabular-nums;
    }
    .kpi small {
      display: block;
      margin-top: 6px;
      color: var(--muted);
      font-size: 12px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .sample-head {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 8px;
      margin-bottom: 12px;
    }
    .sample-head strong {
      font-size: 15px;
      line-height: 1.3;
    }
    .sample-head small {
      margin: 2px 0 0;
      color: var(--muted);
      font-size: 12px;
      text-align: right;
    }
    .sample-metrics {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 7px;
    }
    .sample-metric {
      min-width: 0;
      border: 1px solid #edf1f6;
      border-radius: 8px;
      background: #fbfcfe;
      padding: 7px 8px;
    }
    .sample-metric span {
      margin: 0 0 4px;
      font-size: 11px;
    }
    .sample-metric b {
      display: block;
      font-size: 20px;
      line-height: 1;
      font-variant-numeric: tabular-nums;
    }
    .red { color: var(--red); }
    .green { color: var(--green); }
    .blue { color: var(--blue); }
    .amber { color: var(--amber); }
    .layout {
      display: grid;
      grid-template-columns: minmax(0, 1fr) 440px;
      gap: 12px;
      align-items: start;
    }
    .panel-title {
      display: flex;
      align-items: center;
      justify-content: space-between;
      min-height: 40px;
      padding: 0 13px;
      border-bottom: 1px solid var(--line);
      font-weight: 800;
      color: #1f2937;
    }
    .panel-title small {
      color: var(--muted);
      font-weight: 400;
      font-size: 12px;
    }
    .chart-wrap {
      position: relative;
      min-height: 420px;
      padding: 12px;
    }
    .legend {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 10px;
    }
    .legend span {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      height: 24px;
      padding: 0 8px;
      border: 1px solid var(--line);
      border-radius: 999px;
      background: #fff;
      color: #475569;
      font-size: 12px;
      font-weight: 650;
    }
    .legend i {
      width: 18px;
      height: 3px;
      border-radius: 999px;
      display: inline-block;
    }
    #chart {
      width: 100%;
      min-height: 350px;
      border: 1px solid #edf1f6;
      border-radius: 8px;
      background: linear-gradient(#fff, #fbfcfe);
      overflow: hidden;
    }
    .mini-charts {
      display: grid;
      gap: 10px;
      padding: 10px;
    }
    .mini-chart {
      border: 1px solid #edf1f6;
      border-radius: 8px;
      background: #fff;
      overflow: hidden;
    }
    .mini-chart-title {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 8px 10px 0;
      color: #1f2937;
      font-size: 13px;
      font-weight: 800;
    }
    .mini-chart-title small {
      color: var(--muted);
      font-weight: 400;
      font-size: 12px;
    }
    .chart-tooltip {
      position: fixed;
      top: 0;
      left: 0;
      z-index: 20;
      min-width: 310px;
      pointer-events: none;
      border: 1px solid #cbd5e1;
      border-radius: 8px;
      background: rgba(255, 255, 255, 0.98);
      box-shadow: 0 12px 28px rgba(15, 23, 42, 0.16);
      padding: 8px 10px;
      color: #182033;
      font-size: 12px;
      line-height: 1.55;
      opacity: 0;
      transform: translate(-9999px, -9999px);
      transition: opacity 0.08s ease;
    }
    .chart-tooltip.visible {
      opacity: 1;
    }
    .chart-tooltip strong {
      display: block;
      font-size: 13px;
      margin-bottom: 8px;
    }
    .tooltip-group {
      display: grid;
      grid-template-columns: 86px repeat(2, minmax(0, 1fr));
      gap: 7px;
      align-items: stretch;
      font-variant-numeric: tabular-nums;
    }
    .tooltip-group + .tooltip-group { margin-top: 7px; }
    .tooltip-group b {
      display: flex;
      align-items: center;
      color: #182033;
      font-weight: 850;
    }
    .tooltip-stat {
      min-width: 0;
      border: 1px solid #eef2f7;
      border-radius: 7px;
      background: #fbfcfe;
      padding: 6px 7px;
    }
    .tooltip-stat span {
      display: block;
      color: #64748b;
      font-size: 11px;
      font-weight: 750;
    }
    .tooltip-stat strong {
      margin: 3px 0 0;
      font-size: 16px;
      line-height: 1;
      font-weight: 850;
    }
    .chart-hover-line {
      pointer-events: none;
      stroke: #94a3b8;
      stroke-width: 1;
      stroke-dasharray: 3 3;
      opacity: 0;
    }
    .chart-hit {
      fill: transparent;
      pointer-events: all;
      cursor: crosshair;
    }
    .line-end-label text {
      font-family: inherit;
      font-variant-numeric: tabular-nums;
      pointer-events: none;
    }
    .table-wrap {
      max-height: calc(100vh - 190px);
      overflow: auto;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      font-variant-numeric: tabular-nums;
    }
    th, td {
      padding: 8px 9px;
      border-bottom: 1px solid #edf1f6;
      text-align: right;
      white-space: nowrap;
    }
    th {
      position: sticky;
      top: 0;
      z-index: 1;
      background: #fafafa;
      color: #64748b;
      font-size: 12px;
      font-weight: 800;
    }
    th:nth-child(2), td:nth-child(2) { text-align: left; }
    td:first-child, th:first-child { text-align: center; color: #64748b; }
    .stock {
      display: grid;
      gap: 1px;
      min-width: 0;
    }
    .stock strong {
      color: #182033;
      font-size: 13px;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .stock small {
      color: #94a3b8;
      font-size: 12px;
    }
    .empty, .error {
      padding: 18px;
      color: var(--muted);
    }
    .error { color: #b42318; }
    @media (max-width: 980px) {
      .topbar {
        height: auto;
        min-height: 58px;
        align-items: flex-start;
        flex-direction: column;
        padding: 10px 14px;
      }
      .actions { flex-wrap: wrap; }
      .flow { grid-template-columns: 1fr; }
      .kpis { grid-template-columns: 1fr; }
      .cycle-summary { grid-template-columns: 1fr 1fr; }
      .sh-index-strip { grid-template-columns: 1fr; }
      .cycle-grid { grid-template-columns: 1fr; }
      .cycle-row { grid-template-columns: 1fr; }
      .cycle-count { text-align: left; }
      .layout { grid-template-columns: 1fr; }
      .table-wrap { max-height: none; }
    }
  </style>
</head>
<body>
  <header class="topbar">
    <h1>市场概览</h1>
    <div class="actions">
      <input id="tradeDate" class="date-input" type="date" />
      <button id="refreshBtn" class="btn" type="button">刷新</button>
      <a class="btn" href="/">异动情报流</a>
      <a class="btn" href="/leaders">领头羊</a>
      <span id="refreshText">等待数据</span>
    </div>
  </header>

  <main>
    <section class="service-context" id="serviceContext"></section>
    <section class="flow" id="flow"></section>
    <section class="panel cycle-panel">
      <div class="panel-title">
        指数五日结构
        <small id="cycleHint">看上涨家数、涨停数、&gt;5%、&lt;-5%、跌停数的变化</small>
      </div>
      <div class="cycle-body">
        <div class="cycle-summary" id="cycleSummary"></div>
        <div class="sh-index-strip" id="shIndex5d"></div>
        <div class="cycle-grid" id="cycleGrid"></div>
      </div>
    </section>
    <section class="kpis" id="kpis"></section>
    <section class="layout">
      <section class="panel">
        <div class="panel-title">
          三类样本宽度折线
          <small id="chartHint">全市场 / 成交额Top50 / 研究池，只看上涨/下跌</small>
        </div>
        <div class="chart-wrap">
          <div class="legend" id="legend"></div>
          <div id="chart"></div>
          <div class="chart-tooltip" id="chartTooltip"></div>
        </div>
      </section>
      <section class="panel">
        <div class="panel-title">
          成交额 Top50
          <small id="topHint"></small>
        </div>
        <div class="table-wrap">
          <table>
            <thead>
              <tr><th>#</th><th>股票</th><th>涨幅</th><th>成交额</th></tr>
            </thead>
            <tbody id="top50"></tbody>
          </table>
        </div>
      </section>
    </section>
  </main>

  <script>
    const $ = id => document.getElementById(id);
    const esc = value => String(value ?? "").replace(/[&<>"']/g, c => ({
      "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;"
    }[c]));
    const clean = value => String(value ?? "").replace(/[ \t\r\f\v]+/g, " ").trim();
    const num = value => Number(value || 0);
    const fmt = value => Number(value || 0).toLocaleString("zh-CN");
    const fmtYi = value => `${Number(value || 0).toFixed(2)}亿`;
    const stockUrl = code => `https://stockpage.10jqka.com.cn/${encodeURIComponent(String(code || ""))}/`;
    function timeText(value) {
      if (!value) return "";
      const parts = String(value).split(" ");
      return parts[1] || parts[0] || "";
    }
    function renderServiceContext(data) {
      const ctx = data.service_context || {};
      const chips = [
        `${ctx.service_trade_date || data.trade_date || "最近交易日"} ${ctx.phase_label || "观察"}`,
        ctx.base_trade_date ? `底稿 ${ctx.base_trade_date} 收盘` : "",
        data.market_data_trade_date && data.market_data_trade_date !== data.trade_date ? `概览数据 ${data.market_data_trade_date}` : "",
        ctx.latest_realtime_at ? `实时 ${timeText(ctx.latest_realtime_at)}` : "",
        ctx.post_close_status_label ? `盘后 ${ctx.post_close_status_label}` : ""
      ].filter(Boolean);
      $("serviceContext").innerHTML = chips.map(item => `<span>${esc(item)}</span>`).join("");
    }
    const seriesMeta = [
      { key: "up_count", label: "上涨家数", color: "#d14545" },
      { key: "down_count", label: "下跌家数", color: "#15916f" }
    ];
    const sampleDefs = [
      {
        key: "market",
        title: "全市场",
        hint: "沪深A股，过滤ST/退市/北交所",
        count: "total_count",
        up: "up_count",
        down: "down_count",
        flat: "flat_count",
        up3: "up3_count",
        down3: "down3_count",
        up5: "up5_count",
        down5: "down5_count",
        limitUp: "limit_up_count",
        limitDown: "limit_down_count"
      },
      {
        key: "amount_top50",
        title: "成交额Top50",
        hint: "当前快照成交额最大50只",
        count: "amount_top50_count",
        up: "amount_top50_up_count",
        down: "amount_top50_down_count",
        flat: "amount_top50_flat_count",
        up3: "amount_top50_up3_count",
        down3: "amount_top50_down3_count",
        up5: "amount_top50_up5_count",
        down5: "amount_top50_down5_count",
        limitUp: "",
        limitDown: ""
      },
      {
        key: "research_pool",
        title: "研究池",
        hint: "近5日涨停 + 5日涨幅Top30研究池",
        count: "research_pool_count",
        up: "research_pool_up_count",
        down: "research_pool_down_count",
        flat: "research_pool_flat_count",
        up3: "research_pool_up3_count",
        down3: "research_pool_down3_count",
        up5: "research_pool_up5_count",
        down5: "research_pool_down5_count",
        limitUp: "",
        limitDown: ""
      }
    ];
    function queryDate() {
      return $("tradeDate").value || "";
    }
    let currentChartRows = [];
    let currentChartRowsByKey = {};
    function isTradingTime(value) {
      const text = String(value || "");
      const match = text.match(/(\d{2}):(\d{2})(?::\d{2})?/);
      if (!match) return false;
      const minute = Number(match[1]) * 60 + Number(match[2]);
      return (minute >= 9 * 60 + 30 && minute <= 11 * 60 + 30)
        || (minute >= 13 * 60 && minute <= 15 * 60);
    }
    function normalizeWidthRow(row) {
      if (!row || typeof row !== "object") return row;
      return { ...row };
    }
    function normalizeSeries(rows) {
      return (Array.isArray(rows) ? rows : [])
        .filter(row => row && row.captured_at)
        .map(normalizeWidthRow)
        .filter(row => isTradingTime(row.captured_at || row.time))
        .sort((a, b) => String(a.captured_at).localeCompare(String(b.captured_at)));
    }
    function renderFlow(latest, series) {
      const first = series[0]?.captured_at || "-";
      const last = series[series.length - 1]?.captured_at || latest?.captured_at || "-";
      const steps = [
        ["1 数据采集", latest?.source || "akshare_stock_zh_a_spot", `快照 ${latest?.captured_at || "-"}`],
        ["2 样本拆分", "全市场 / 成交额Top50 / 研究池", `研究池 ${latest?.research_pool_trade_date || "-"}`],
        ["3 统计入库", "涨跌家数、>5%、<-5%、涨跌停", `序列 ${series.length} 个时间点`],
        ["4 趋势展示", "仅交易时段，从左到右", `${first} -> ${last}`]
      ];
      $("flow").innerHTML = steps.map(([label, title, sub]) => `
        <div class="flow-step">
          <span>${esc(label)}</span>
          <strong>${esc(title)}</strong>
          <small>${esc(sub)}</small>
        </div>
      `).join("");
    }
    function sampleHint(def, latest) {
      if (def.key !== "research_pool") return def.hint;
      const day = latest?.research_pool_trade_date || "-";
      return `${day} 研究池`;
    }
    function chartRowsForDef(rows, def) {
      return Array.isArray(rows) ? rows : [];
    }
    function chartSampleNote(allRows, chartRows, def) {
      return "";
    }
    function renderKpis(latest) {
      $("kpis").innerHTML = sampleDefs.map(def => `
        <div class="kpi">
          <div class="sample-head">
            <strong>${esc(def.title)}</strong>
            <small>${esc(sampleHint(def, latest))}<br>样本 ${fmt(latest?.[def.count])}</small>
          </div>
          <div class="sample-metrics">
            <div class="sample-metric"><span>上涨</span><b class="red">${fmt(latest?.[def.up])}</b></div>
            <div class="sample-metric"><span>下跌</span><b class="green">${fmt(latest?.[def.down])}</b></div>
            <div class="sample-metric"><span>>5%</span><b class="amber">${fmt(latest?.[def.up5])}</b></div>
            <div class="sample-metric"><span><-5%</span><b class="blue">${fmt(latest?.[def.down5])}</b></div>
          </div>
        </div>
      `).join("");
    }
    function signed(value) {
      const n = Number(value || 0);
      if (n > 0) return `+${fmt(n)}`;
      return fmt(n);
    }
    function signedPct(value) {
      const n = Number(value || 0);
      if (!Number.isFinite(n)) return "-";
      return `${n > 0 ? "+" : ""}${n.toFixed(2)}%`;
    }
    function fmtVolumeYi(value) {
      const n = Number(value || 0);
      if (!Number.isFinite(n) || n <= 0) return "成交量 -";
      return `成交量 ${(n / 100000000).toFixed(2)}亿股`;
    }
    function trendClass(value) {
      const n = Number(value || 0);
      return n > 0 ? "red" : n < 0 ? "green" : "";
    }
    function dayPower(row, side) {
      if (!row) return 0;
      if (side === "positive") return num(row.up_count) + num(row.up5_count) * 4 + num(row.limit_up_count) * 12;
      return num(row.down_count) + num(row.down5_count) * 4 + num(row.limit_down_count) * 12;
    }
    function powerParts(row, side) {
      if (side === "positive") {
        return [
          { label: "上涨", raw: num(row.up_count), value: num(row.up_count), cls: "seg-up", color: "#f3a0a0" },
          { label: ">5", raw: num(row.up5_count), value: num(row.up5_count) * 4, cls: "seg-up5", color: "#d14545" },
          { label: "涨停", raw: num(row.limit_up_count), value: num(row.limit_up_count) * 12, cls: "seg-limit-up", color: "#8f1d1d" }
        ];
      }
      return [
        { label: "下跌", raw: num(row.down_count), value: num(row.down_count), cls: "seg-down", color: "#8ad2bf" },
        { label: "<-5", raw: num(row.down5_count), value: num(row.down5_count) * 4, cls: "seg-down5", color: "#15916f" },
        { label: "跌停", raw: num(row.limit_down_count), value: num(row.limit_down_count) * 12, cls: "seg-limit-down", color: "#0b5d49" }
      ];
    }
    function renderPowerStack(parts, maxPower) {
      const total = parts.reduce((sum, item) => sum + item.value, 0);
      const width = total > 0 ? Math.max(2, total / Math.max(1, maxPower) * 100) : 0;
      const segments = parts.map(item => {
        const pct = total > 0 ? item.value / total * 100 : 0;
        return `<div class="bar-seg ${item.cls}" style="width:${pct.toFixed(2)}%" title="${esc(item.label)} ${fmt(item.raw)}"></div>`;
      }).join("");
      return `<div class="bar-stack" style="width:${width.toFixed(1)}%">${segments}</div>`;
    }
    function renderComponentLine(parts) {
      return parts.map(item => `<span><i class="dot" style="background:${item.color}"></i>${esc(item.label)} ${fmt(item.raw)}</span>`).join("  ");
    }
    function cycleLabel(rows) {
      if (!Array.isArray(rows) || rows.length < 2) return ["数据不足", "需要至少两个交易日快照"];
      const last = rows[rows.length - 1];
      const prev = rows[rows.length - 2];
      const upDelta = num(last.up_count) - num(prev.up_count);
      const up5Delta = num(last.up5_count) - num(prev.up5_count);
      const limitUpDelta = num(last.limit_up_count) - num(prev.limit_up_count);
      const down5Delta = num(last.down5_count) - num(prev.down5_count);
      const limitDownDelta = num(last.limit_down_count) - num(prev.limit_down_count);
      if (upDelta > 0 && up5Delta >= 0 && down5Delta <= 0) {
        return ["正向修复增强", "上涨家数增加，强势股没有收缩"];
      }
      if (upDelta >= 0 && (up5Delta < 0 || limitUpDelta < 0) && down5Delta <= 0) {
        return ["上涨延续降速", "方向还在修复，但5%强势股或涨停开始收敛"];
      }
      if (down5Delta < 0 && limitDownDelta <= 0) {
        return ["下跌延续收敛", "仍有分歧，但<-5%和跌停压力在减弱"];
      }
      if (down5Delta > 0 || limitDownDelta > 0) {
        return ["分歧压力扩大", "<-5%或跌停数量继续增加"];
      }
      return ["结构过渡", "正负动能变化不够一致"];
    }
    function renderShIndex5d(rows) {
      const items = (Array.isArray(rows) ? rows : [])
        .filter(row => row && row.sh_index_price !== null && row.sh_index_price !== undefined)
        .slice(-5);
      if (!items.length) {
        $("shIndex5d").innerHTML = "";
        return;
      }
      $("shIndex5d").innerHTML = items.map(row => {
        const pct = Number(row.sh_index_pct_change || 0);
        const cls = trendClass(pct);
        const amount = Number(row.total_amount_yi || 0);
        const liquidity = amount > 0 ? `全市场成交额 ${amount.toFixed(2)}亿` : "全市场成交额 -";
        return `<div class="sh-index-day">
          <div class="date"><span>${esc(row.trade_date || "")}</span><span>上证</span></div>
          <strong class="${cls}">${signedPct(pct)}</strong>
          <small>收盘 ${Number(row.sh_index_price || 0).toFixed(2)} · ${esc(liquidity)}</small>
        </div>`;
      }).join("");
    }
    function renderCycle5d(rows) {
      const list = (Array.isArray(rows) ? rows : [])
        .filter(Boolean)
        .sort((a, b) => String(a.trade_date || "").localeCompare(String(b.trade_date || "")));
      if (!list.length) {
        $("cycleSummary").innerHTML = `<div class="cycle-card"><strong>暂无五日结构</strong><small>先运行市场概览任务</small></div>`;
        $("shIndex5d").innerHTML = "";
        $("cycleGrid").innerHTML = "";
        return;
      }
      renderShIndex5d(list);
      const last = list[list.length - 1];
      const prev = list.length >= 2 ? list[list.length - 2] : null;
      const [label, note] = cycleLabel(list);
      const upDelta = prev ? num(last.up_count) - num(prev.up_count) : 0;
      const up5Delta = prev ? num(last.up5_count) - num(prev.up5_count) : 0;
      const down5Delta = prev ? num(last.down5_count) - num(prev.down5_count) : 0;
      const limitDownDelta = prev ? num(last.limit_down_count) - num(prev.limit_down_count) : 0;
      $("cycleSummary").innerHTML = `
        <div class="cycle-card">
          <span>结构判断</span>
          <strong>${esc(label)}</strong>
          <small>${esc(note)}</small>
        </div>
        <div class="cycle-card">
          <span>上涨家数变化</span>
          <strong class="${trendClass(upDelta)}">${signed(upDelta)}</strong>
          <small>今日 ${fmt(last.up_count)} / 样本 ${fmt(last.total_count)}</small>
        </div>
        <div class="cycle-card">
          <span>>5% 强势变化</span>
          <strong class="${trendClass(up5Delta)}">${signed(up5Delta)}</strong>
          <small>今日 ${fmt(last.up5_count)}，涨停 ${fmt(last.limit_up_count)}</small>
        </div>
        <div class="cycle-card">
          <span><-5% 风险变化</span>
          <strong class="${trendClass(-down5Delta)}">${signed(down5Delta)}</strong>
          <small>今日 ${fmt(last.down5_count)}，跌停 ${fmt(last.limit_down_count)}</small>
        </div>
        <div class="cycle-card">
          <span>跌停变化</span>
          <strong class="${trendClass(-limitDownDelta)}">${signed(limitDownDelta)}</strong>
          <small>数据日 ${esc(last.trade_date || "")}</small>
        </div>
      `;
      const maxPower = Math.max(10, ...list.flatMap(row => [dayPower(row, "positive"), dayPower(row, "negative")]));
      $("cycleGrid").innerHTML = list.map(row => {
        const positive = dayPower(row, "positive");
        const negative = dayPower(row, "negative");
        const positiveParts = powerParts(row, "positive");
        const negativeParts = powerParts(row, "negative");
        const dataNote = row.has_5pct_data ? "" : `<span class="data-missing">5%历史缺失</span>`;
        return `<div class="cycle-row">
          <div class="cycle-date">${esc(row.trade_date)}${dataNote}</div>
          <div class="power-bar">
            <div class="power-head"><span>正向：上涨 + &gt;5% + 涨停</span><b class="red">${fmt(positive)}</b></div>
            <div class="bar-track">${renderPowerStack(positiveParts, maxPower)}</div>
            <div class="component-line">${renderComponentLine(positiveParts)}</div>
          </div>
          <div class="power-bar">
            <div class="power-head"><span>负向：下跌 + &lt;-5% + 跌停</span><b class="green">${fmt(negative)}</b></div>
            <div class="bar-track">${renderPowerStack(negativeParts, maxPower)}</div>
            <div class="component-line">${renderComponentLine(negativeParts)}</div>
          </div>
          <div class="cycle-count positive"><span>&gt;5 / 涨停</span><b>${fmt(row.up5_count)} / ${fmt(row.limit_up_count)}</b></div>
          <div class="cycle-count negative"><span>&lt;-5 / 跌停</span><b>${fmt(row.down5_count)} / ${fmt(row.limit_down_count)}</b></div>
        </div>`;
      }).join("");
    }
    function renderLegend() {
      $("legend").innerHTML = seriesMeta.map(item => `
        <span><i style="background:${item.color}"></i>${esc(item.label)}</span>
      `).join("");
    }
    function renderOneChart(rows, def) {
      const chartRows = chartRowsForDef(rows, def);
      const mapped = [
        { key: def.up, label: "上涨家数", color: "#d14545" },
        { key: def.down, label: "下跌家数", color: "#15916f" }
      ];
      const width = 980;
      const height = 210;
      const pad = { left: 50, right: 104, top: 18, bottom: 30 };
      const plotW = width - pad.left - pad.right;
      const plotH = height - pad.top - pad.bottom;
      const maxY = Math.max(10, ...chartRows.flatMap(row => mapped.map(item => num(row[item.key]))));
      const x = index => pad.left + (chartRows.length === 1 ? plotW / 2 : index * plotW / (chartRows.length - 1));
      const y = value => pad.top + plotH - num(value) / maxY * plotH;
      const grid = [0, 0.5, 1].map(rate => {
        const gy = pad.top + plotH - rate * plotH;
        const val = Math.round(maxY * rate);
        return `<line x1="${pad.left}" y1="${gy}" x2="${width - pad.right}" y2="${gy}" stroke="#edf1f6"/><text x="8" y="${gy + 4}" fill="#94a3b8" font-size="11">${val}</text>`;
      }).join("");
      const lines = mapped.map(item => {
        const points = chartRows.map((row, index) => `${x(index).toFixed(1)},${y(row[item.key]).toFixed(1)}`).join(" ");
        const dots = chartRows.map((row, index) => `<circle cx="${x(index).toFixed(1)}" cy="${y(row[item.key]).toFixed(1)}" r="2.2" fill="${item.color}"><title>${item.label} ${row.time}: ${row[item.key]}</title></circle>`).join("");
        return `<polyline fill="none" stroke="${item.color}" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" points="${points}"/>${dots}`;
      }).join("");
      const endLabels = chartRows.length ? mapped.map((item, index) => {
        const last = chartRows[chartRows.length - 1];
        const rawY = y(last[item.key]);
        const labelY = Math.min(height - pad.bottom - 34, Math.max(pad.top + 2, rawY - 17 + index * 34));
        const labelX = width - pad.right + 10;
        return `<g class="line-end-label">
          <line x1="${x(chartRows.length - 1).toFixed(1)}" y1="${rawY.toFixed(1)}" x2="${(labelX - 5).toFixed(1)}" y2="${(labelY + 17).toFixed(1)}" stroke="${item.color}" stroke-width="1" stroke-opacity="0.35"/>
          <rect x="${labelX}" y="${labelY.toFixed(1)}" width="80" height="32" rx="7" fill="#fff" stroke="${item.color}" stroke-opacity="0.38"/>
          <text x="${labelX + 8}" y="${(labelY + 12).toFixed(1)}" fill="#64748b" font-size="10" font-weight="700">${esc(item.label)}</text>
          <text x="${labelX + 8}" y="${(labelY + 26).toFixed(1)}" fill="${item.color}" font-size="14" font-weight="850">${fmt(last[item.key])}</text>
        </g>`;
      }).join("") : "";
      const labelIndexes = [...new Set([0, Math.floor((chartRows.length - 1) / 2), chartRows.length - 1])].filter(index => index >= 0);
      const labels = labelIndexes.map(index => `<text x="${x(index).toFixed(1)}" y="${height - 10}" text-anchor="middle" fill="#64748b" font-size="11">${esc(chartRows[index].time)}</text>`).join("");
      const latestText = chartRows.length ? `${chartRows[0].time} -> ${chartRows[chartRows.length - 1].time}` : "";
      const sampleNote = chartSampleNote(rows, chartRows, def);
      const titleText = sampleNote ? `${latestText} · ${sampleNote}` : latestText;
      const hitWidth = chartRows.length <= 1 ? plotW : plotW / Math.max(1, chartRows.length - 1);
      const hits = chartRows.map((row, index) => {
        const cx = x(index);
        const left = Math.max(pad.left, cx - hitWidth / 2);
        const right = Math.min(width - pad.right, cx + hitWidth / 2);
        return `<g>
          <line class="chart-hover-line" data-chart-key="${esc(def.key)}" data-hover-index="${index}" x1="${cx.toFixed(1)}" y1="${pad.top}" x2="${cx.toFixed(1)}" y2="${pad.top + plotH}"></line>
          <rect class="chart-hit" x="${left.toFixed(1)}" y="${pad.top}" width="${Math.max(8, right - left).toFixed(1)}" height="${plotH}" data-index="${index}"></rect>
        </g>`;
      }).join("");
      return `<div class="mini-chart">
        <div class="mini-chart-title">${esc(def.title)}<small>${esc(titleText)}</small></div>
        <svg data-width-chart="1" data-chart-key="${esc(def.key)}" viewBox="0 0 ${width} ${height}" width="100%" height="210" role="img" aria-label="${esc(def.title)}宽度折线图">${grid}${lines}${endLabels}${labels}${hits}</svg>
      </div>`;
    }
    function renderChart(rows) {
      if (!Array.isArray(rows) || rows.length === 0) {
        $("chart").innerHTML = `<div class="empty">暂无概览快照，先运行市场概览任务。</div>`;
        return;
      }
      currentChartRows = rows;
      currentChartRowsByKey = Object.fromEntries(sampleDefs.map(def => [def.key, chartRowsForDef(rows, def)]));
      $("chart").innerHTML = `<div class="mini-charts">${sampleDefs.map(def => renderOneChart(rows, def)).join("")}</div>`;
    }
    function showChartTooltip(event, row) {
      const tooltip = $("chartTooltip");
      if (!row) return;
      tooltip.innerHTML = `
        <strong>${esc(row.captured_at || row.time || "")}</strong>
        ${sampleDefs.map(def => `
          <div class="tooltip-group">
            <b>${esc(def.title)}</b>
            <div class="tooltip-stat"><span>上涨</span><strong class="red">${fmt(row[def.up])}</strong></div>
            <div class="tooltip-stat"><span>下跌</span><strong class="green">${fmt(row[def.down])}</strong></div>
          </div>
        `).join("")}`;
      const x = Math.min(window.innerWidth - 330, event.clientX + 14);
      const y = Math.min(window.innerHeight - 170, event.clientY + 14);
      tooltip.style.transform = `translate(${Math.max(8, x)}px, ${Math.max(8, y)}px)`;
      tooltip.classList.add("visible");
    }
    function hideChartTooltip() {
      const tooltip = $("chartTooltip");
      tooltip.classList.remove("visible");
      tooltip.style.transform = "translate(-9999px, -9999px)";
      document.querySelectorAll(".chart-hover-line").forEach(line => line.style.opacity = "0");
    }
    function highlightChartIndex(index) {
      document.querySelectorAll(".chart-hover-line").forEach(line => line.style.opacity = "0");
      const chartKey = arguments.length > 1 ? arguments[1] : "";
      const selector = chartKey
        ? `.chart-hover-line[data-chart-key="${CSS.escape(chartKey)}"][data-hover-index="${index}"]`
        : `.chart-hover-line[data-hover-index="${index}"]`;
      document.querySelectorAll(selector).forEach(line => line.style.opacity = "1");
    }
    function indexFromSvgPointer(svg, event) {
      const chartKey = String(svg.dataset.chartKey || "").trim();
      const rows = currentChartRowsByKey[chartKey] || currentChartRows;
      if (!rows.length) return -1;
      const width = 980;
      const padLeft = 50;
      const padRight = 104;
      const plotW = width - padLeft - padRight;
      const rect = svg.getBoundingClientRect();
      if (!rect.width) return -1;
      const vx = (event.clientX - rect.left) / rect.width * width;
      const ratio = Math.min(1, Math.max(0, (vx - padLeft) / plotW));
      return Math.min(rows.length - 1, Math.max(0, Math.round(ratio * (rows.length - 1))));
    }
    document.addEventListener("pointermove", event => {
      const svg = event.target.closest?.("svg[data-width-chart]");
      if (!svg) return;
      const chartKey = String(svg.dataset.chartKey || "").trim();
      const rows = currentChartRowsByKey[chartKey] || currentChartRows;
      const index = indexFromSvgPointer(svg, event);
      if (index < 0) return;
      highlightChartIndex(index, chartKey);
      showChartTooltip(event, rows[index]);
    });
    document.addEventListener("pointerout", event => {
      const chart = event.target.closest?.("#chart");
      if (!chart) return;
      const related = event.relatedTarget;
      if (!related || !chart.contains(related)) hideChartTooltip();
    });
    function renderTop50(rows) {
      const list = Array.isArray(rows) ? rows : [];
      if (!list.length) {
        $("top50").innerHTML = `<tr><td colspan="4" class="empty">暂无Top50数据</td></tr>`;
        return;
      }
      $("top50").innerHTML = list.map(row => {
        const pct = num(row.pct_change);
        const cls = pct > 0 ? "red" : pct < 0 ? "green" : "";
        return `<tr>
          <td>${esc(row.rank_no)}</td>
          <td><a class="stock" href="${stockUrl(row.code)}" target="_blank" rel="noopener noreferrer"><strong>${esc(row.name)}</strong><small>${esc(row.code)}</small></a></td>
          <td class="${cls}">${pct.toFixed(2)}%</td>
          <td>${fmtYi(row.amount_yi)}</td>
        </tr>`;
      }).join("");
      $("topHint").textContent = `共 ${list.length} 只`;
    }
    async function load() {
      try {
        const params = new URLSearchParams();
        if (queryDate()) params.set("trade_date", queryDate());
        const response = await fetch(`/api/market-width?${params.toString()}`, { cache: "no-store" });
        const data = await response.json();
        const latest = normalizeWidthRow(data.latest || {});
        const series = normalizeSeries(data.series);
        if (!$("tradeDate").value && data.trade_date) $("tradeDate").value = data.trade_date;
        renderServiceContext(data);
        renderFlow(latest, series);
        renderCycle5d((data.cycle_5d || []).map(normalizeWidthRow));
        renderKpis(latest);
        renderLegend();
        renderChart(series);
        renderTop50(data.top50 || []);
        $("refreshText").textContent = `已刷新 ${new Date().toLocaleTimeString("zh-CN", { hour12: false })}`;
      } catch (error) {
        $("chart").innerHTML = `<div class="error">${esc(error)}</div>`;
        $("refreshText").textContent = "刷新失败";
      }
    }
    $("refreshBtn").addEventListener("click", load);
    $("tradeDate").addEventListener("change", load);
    load();
    setInterval(load, 60000);
  </script>
</body>
</html>"""


LEADERS_HTML = r"""<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>领头羊</title>
  <style>
    :root {
      color-scheme: light;
      --bg: #f5f7fb;
      --panel: #ffffff;
      --line: #e5e7eb;
      --text: #182033;
      --muted: #64748b;
      --red: #d14545;
      --green: #15916f;
      --blue: #2563eb;
      --amber: #b7791f;
      --shadow: 0 8px 20px rgba(15, 23, 42, 0.05);
      font-family: "Microsoft YaHei", "PingFang SC", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      background: var(--bg);
      color: var(--text);
      font-size: 14px;
      letter-spacing: 0;
    }
    .topbar {
      position: sticky;
      top: 0;
      z-index: 10;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      min-height: 58px;
      padding: 10px 18px;
      border-bottom: 1px solid var(--line);
      background: rgba(255, 255, 255, 0.96);
      backdrop-filter: blur(12px);
    }
    h1 {
      margin: 0;
      font-size: 19px;
      line-height: 1.2;
    }
    .actions {
      display: flex;
      align-items: center;
      gap: 8px;
      color: var(--muted);
      font-size: 12px;
    }
    .btn, .date-input {
      height: 32px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: #fff;
      color: #334155;
      padding: 0 10px;
      font: inherit;
      font-size: 13px;
      text-decoration: none;
    }
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      font-weight: 750;
    }
    main {
      max-width: 1380px;
      margin: 0 auto;
      padding: 14px;
    }
    .service-context {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 12px;
      color: var(--muted);
      font-size: 12px;
    }
    .service-context span {
      display: inline-flex;
      align-items: center;
      min-height: 28px;
      padding: 4px 9px;
      border: 1px solid var(--line);
      border-radius: 999px;
      background: #fff;
    }
    .scope {
      border: 1px solid var(--line);
      border-radius: 10px;
      background: var(--panel);
      box-shadow: var(--shadow);
    }
    .leader-workspace {
      display: grid;
      grid-template-columns: 300px minmax(0, 1fr);
      gap: 12px;
      align-items: start;
    }
    .theme-panel, .scope {
      border: 1px solid var(--line);
      border-radius: 10px;
      background: var(--panel);
      box-shadow: var(--shadow);
    }
    .theme-panel {
      position: sticky;
      top: 72px;
      max-height: calc(100vh - 88px);
      overflow: hidden;
    }
    .theme-panel-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      min-height: 46px;
      padding: 10px 12px;
      border-bottom: 1px solid var(--line);
      background: #fbfcfe;
    }
    .theme-panel-head strong {
      font-size: 15px;
    }
    .theme-count {
      color: var(--muted);
      font-size: 12px;
      font-variant-numeric: tabular-nums;
    }
    .theme-search-wrap {
      padding: 8px 8px 0;
      background: #fff;
    }
    .theme-search {
      width: 100%;
      height: 32px;
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      background: #f8fafc;
      color: var(--text);
      padding: 0 10px;
      font: inherit;
      font-size: 13px;
      outline: none;
    }
    .theme-search:focus {
      border-color: #93b7f4;
      background: #fff;
      box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.08);
    }
    .theme-tabs {
      display: grid;
      gap: 6px;
      max-height: calc(100vh - 176px);
      overflow: auto;
      padding: 8px;
    }
    .theme-tab {
      width: 100%;
      min-height: 58px;
      border: 1px solid #e8edf4;
      border-radius: 8px;
      background: #fff;
      color: var(--text);
      padding: 8px 9px;
      text-align: left;
      cursor: pointer;
      font: inherit;
    }
    .theme-tab:hover {
      border-color: #c7d7f4;
      background: #f8fbff;
    }
    .theme-tab.active {
      border-color: #93b7f4;
      background: #eef6ff;
      box-shadow: inset 3px 0 0 var(--blue);
    }
    .theme-tab:first-child {
      border-color: #f1d4ae;
      background: #fffaf1;
    }
    .theme-tab:first-child.active {
      border-color: #e3b86f;
      background: #fff4da;
      box-shadow: inset 3px 0 0 var(--amber);
    }
    .theme-tab-top {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 8px;
      align-items: center;
    }
    .theme-tab-name {
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-size: 14px;
      font-weight: 850;
    }
    .theme-tab-score {
      color: var(--red);
      font-size: 13px;
      font-weight: 900;
      font-variant-numeric: tabular-nums;
    }
    .theme-tab-sub {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      margin-top: 6px;
      color: var(--muted);
      font-size: 12px;
      font-variant-numeric: tabular-nums;
    }
    .theme-tab-leader {
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .scope-stage {
      min-width: 0;
    }
    .scope {
      overflow: hidden;
    }
    .scope-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      min-height: 46px;
      padding: 10px 13px;
      border-bottom: 1px solid var(--line);
      background: #fbfcfe;
    }
    .scope-title {
      min-width: 0;
    }
    .scope-title strong {
      display: block;
      font-size: 16px;
      line-height: 1.2;
    }
    .scope-title small {
      display: block;
      margin-top: 4px;
      color: var(--muted);
      font-size: 12px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .scope-meta {
      display: flex;
      flex-wrap: wrap;
      justify-content: flex-end;
      gap: 6px;
      color: #475569;
      font-size: 12px;
      font-variant-numeric: tabular-nums;
    }
    .pill {
      display: inline-flex;
      align-items: center;
      min-height: 24px;
      border: 1px solid #e2e8f0;
      border-radius: 999px;
      background: #fff;
      padding: 0 8px;
      font-weight: 750;
    }
    .scope-overview {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 8px;
      padding: 12px 12px 0;
    }
    .overview-cell {
      min-width: 0;
      border: 1px solid #edf1f6;
      border-radius: 8px;
      background: #fbfcfe;
      padding: 9px 10px;
      font-variant-numeric: tabular-nums;
    }
    .overview-cell span {
      display: block;
      color: var(--muted);
      font-size: 11px;
      white-space: nowrap;
    }
    .overview-cell strong {
      display: block;
      margin-top: 5px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-size: 16px;
      line-height: 1.15;
    }
    .overview-cell.hot strong {
      color: var(--red);
      font-size: 20px;
    }
    .leaders {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 8px;
      padding: 12px;
    }
    .leader-group {
      display: grid;
      gap: 8px;
      padding: 12px 12px 0;
    }
    .leader-group + .leader-group {
      padding-top: 0;
    }
    .leader-group-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 8px;
      color: #334155;
      font-size: 13px;
      font-weight: 850;
    }
    .leader-group-head small {
      color: var(--muted);
      font-size: 12px;
      font-weight: 650;
    }
    .leader-group .leaders {
      padding: 0;
    }
    .leader-group.trend .leaders {
      grid-template-columns: minmax(0, 1fr);
      max-width: 440px;
    }
    .leader {
      position: relative;
      min-width: 0;
      border: 1px solid #edf1f6;
      border-left: 3px solid #d14545;
      border-radius: 8px;
      background: #fff;
      padding: 10px 12px;
      display: grid;
      grid-template-columns: 58px minmax(150px, 210px) minmax(230px, 0.85fr) minmax(280px, 1.15fr);
      gap: 12px;
      align-items: center;
      box-shadow: 0 1px 0 rgba(15, 23, 42, 0.03);
    }
    .leader + .leader {
      margin-top: 2px;
    }
    .leader.primary {
      border-color: #f1c7c7;
      border-left-color: var(--red);
      background: linear-gradient(90deg, #fff8f8 0%, #ffffff 38%);
      box-shadow: 0 8px 18px rgba(209, 69, 69, 0.08);
    }
    .leader.rank-2 {
      border-color: #ead7b7;
      border-left-color: #c9852f;
      background: linear-gradient(90deg, #fffaf1 0%, #ffffff 34%);
      box-shadow: 0 6px 16px rgba(201, 133, 47, 0.08);
    }
    .leader.rank-3 {
      border-color: #cfd9ea;
      border-left-color: #5475a8;
      background: linear-gradient(90deg, #f7faff 0%, #ffffff 34%);
      box-shadow: 0 6px 16px rgba(84, 117, 168, 0.07);
    }
    .leader.trend {
      border-color: #b9dfd4;
      border-left-color: var(--green);
      background: linear-gradient(90deg, #f5fbf8 0%, #ffffff 34%);
      box-shadow: 0 8px 18px rgba(21, 145, 111, 0.07);
    }
    .rank-cell {
      height: 100%;
      display: grid;
      justify-items: center;
      align-content: center;
      gap: 6px;
      padding-right: 10px;
      border-right: 1px solid #eef2f7;
    }
    .leader-identity {
      min-width: 0;
      display: grid;
      gap: 5px;
    }
    .leader-signal {
      min-width: 0;
      display: grid;
      gap: 7px;
    }
    .score-line {
      display: flex;
      align-items: center;
      gap: 8px;
      color: var(--muted);
      font-size: 12px;
      font-variant-numeric: tabular-nums;
    }
    .score-line strong {
      color: var(--red);
      font-size: 20px;
      line-height: 1;
    }
    .leader.trend .score-line strong {
      color: var(--green);
    }
    .rank {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 34px;
      height: 34px;
      border-radius: 8px;
      background: #eef6ff;
      color: var(--blue);
      font-size: 18px;
      font-weight: 900;
      font-variant-numeric: tabular-nums;
    }
    .leader.primary .rank {
      background: #ffe8e8;
      color: var(--red);
    }
    .leader.rank-2 .rank {
      background: #fff0d2;
      color: #a66216;
    }
    .leader.rank-3 .rank {
      background: #eaf1ff;
      color: #3d6096;
    }
    .leader.trend .rank {
      background: #eaf8f1;
      color: var(--green);
    }
    .stock {
      min-width: 0;
      color: inherit;
      text-decoration: none;
    }
    .stock strong {
      display: block;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-size: 15px;
    }
    .stock small {
      display: block;
      margin-top: 3px;
      color: #94a3b8;
      font-size: 12px;
      font-variant-numeric: tabular-nums;
    }
    .score {
      text-align: right;
      font-variant-numeric: tabular-nums;
    }
    .score strong {
      display: block;
      color: var(--red);
      font-size: 24px;
      line-height: 1;
    }
    .score small {
      display: block;
      margin-top: 4px;
      color: var(--muted);
      font-size: 11px;
    }
    .type-badge {
      display: inline-flex;
      align-items: center;
      width: max-content;
      border: 1px solid #e2e8f0;
      border-radius: 999px;
      background: #fff;
      color: #64748b;
      padding: 1px 7px;
      font-size: 11px;
      font-weight: 800;
    }
    .type-badge.trend {
      border-color: #b9dfd4;
      color: var(--green);
      background: #f4fbf8;
    }
    .score-grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 5px;
    }
    .score-grid.compact {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
    .score-cell {
      min-width: 0;
      border: 1px solid #eef2f7;
      border-radius: 7px;
      background: #fbfcfe;
      padding: 6px 7px;
      text-align: center;
      font-variant-numeric: tabular-nums;
    }
    .score-cell span {
      display: block;
      color: var(--muted);
      font-size: 11px;
      white-space: nowrap;
    }
    .score-cell b {
      display: block;
      margin-top: 4px;
      font-size: 15px;
      line-height: 1;
    }
    .dim-list {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
    }
    .dim {
      min-width: 0;
      display: inline-flex;
      align-items: center;
      gap: 5px;
      min-height: 24px;
      border-radius: 999px;
      background: #fbfcfe;
      border: 1px solid #eef2f7;
      padding: 1px 7px;
      color: #334155;
      font-size: 11px;
      font-variant-numeric: tabular-nums;
    }
    .dim span {
      color: #475569;
      font-weight: 750;
    }
    .dim b {
      color: var(--red);
      text-align: center;
    }
    .dim small {
      min-width: 0;
      color: var(--muted);
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      max-width: 150px;
    }
    .leader-facts {
      display: grid;
      gap: 6px;
      min-width: 0;
    }
    .fact-line {
      border: 1px solid #eef2f7;
      border-radius: 7px;
      background: #fbfcfe;
      padding: 6px 8px;
      color: #334155;
      font-size: 12px;
      line-height: 1.5;
    }
    .fact-line strong {
      display: block;
      margin-bottom: 2px;
      color: #64748b;
      font-size: 11px;
      font-weight: 850;
    }
    .fact-line.theme {
      border-color: #d9eadf;
      background: #f7fcf9;
    }
    .fact-line.highlight {
      border-color: #bdd7ff;
      background: linear-gradient(90deg, #eff6ff 0%, #ffffff 100%);
      color: #1e3a5f;
      box-shadow: inset 3px 0 0 #4f8fdc;
    }
    .fact-line.highlight strong {
      color: #2563eb;
    }
    .fact-line.active {
      border-color: #ecd7bd;
      background: #fffaf3;
    }
    .fact-timeline {
      border: 1px solid #ecd7bd;
      border-radius: 7px;
      background: #fffaf3;
      padding: 7px 8px;
      display: grid;
      gap: 6px;
      color: #334155;
      font-size: 12px;
      line-height: 1.45;
    }
    .fact-timeline strong {
      color: #64748b;
      font-size: 11px;
      font-weight: 850;
    }
    .fact-item {
      display: grid;
      grid-template-columns: 68px minmax(0, 1fr);
      gap: 7px;
      align-items: start;
    }
    .fact-date {
      color: #9a6b2d;
      font-weight: 850;
      font-variant-numeric: tabular-nums;
      white-space: nowrap;
    }
    .fact-text {
      min-width: 0;
      color: #334155;
    }
    .fact-title {
      color: #111827;
      font-weight: 850;
    }
    .fact-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
    }
    .fact-tags span {
      border: 1px solid #e2e8f0;
      border-radius: 999px;
      background: #fff;
      color: #64748b;
      padding: 1px 7px;
      font-size: 11px;
      font-weight: 750;
      line-height: 1.55;
    }
    .empty, .error {
      padding: 18px;
      color: var(--muted);
    }
    .error { color: #b42318; }
    @media (max-width: 980px) {
      .topbar { align-items: flex-start; flex-direction: column; }
      .actions { flex-wrap: wrap; }
      .leader-workspace { grid-template-columns: 1fr; }
      .theme-panel { position: static; max-height: none; }
      .theme-search-wrap { padding: 8px; }
      .theme-tabs {
        display: flex;
        overflow-x: auto;
        max-height: none;
        padding: 0 8px 8px;
      }
      .theme-tab {
        flex: 0 0 210px;
      }
      .scope-overview { grid-template-columns: repeat(2, minmax(0, 1fr)); }
      .leaders, .leader-group.trend .leaders { grid-template-columns: 1fr; max-width: none; }
      .leader {
        grid-template-columns: 46px minmax(0, 1fr);
        align-items: start;
      }
      .leader-signal, .leader-facts {
        grid-column: 1 / -1;
      }
      .scope-head { align-items: flex-start; flex-direction: column; }
      .scope-meta { justify-content: flex-start; }
    }
  </style>
</head>
<body>
  <header class="topbar">
    <h1>领头羊</h1>
    <div class="actions">
      <input id="tradeDate" class="date-input" type="date" />
      <button id="refreshBtn" class="btn" type="button">刷新</button>
      <a class="btn" href="/">异动情报流</a>
      <a class="btn" href="/market-width">市场概览</a>
      <span id="refreshText">等待数据</span>
    </div>
  </header>
  <main>
    <section class="service-context" id="serviceContext"></section>
    <section class="leader-workspace">
      <aside class="theme-panel">
        <div class="theme-panel-head">
          <strong>题材</strong>
          <span class="theme-count" id="scopeCount">0</span>
        </div>
        <div class="theme-search-wrap">
          <input id="scopeSearch" class="theme-search" type="search" placeholder="搜索题材 / 股票" />
        </div>
        <div class="theme-tabs" id="scopeTabs"></div>
      </aside>
      <section class="scope-stage" id="scopeStage"></section>
    </section>
  </main>
  <script>
    const $ = id => document.getElementById(id);
    const esc = value => String(value ?? "").replace(/[&<>"']/g, c => ({
      "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;"
    }[c]));
    const clean = value => String(value ?? "").replace(/[ \t\r\f\v]+/g, " ").trim();
    const num = value => Number(value || 0);
    const fmt = value => Number(value || 0).toLocaleString("zh-CN");
    const pct = value => {
      const n = Number(value || 0);
      return `${n > 0 ? "+" : ""}${n.toFixed(2)}%`;
    };
    const stockUrl = code => `https://stockpage.10jqka.com.cn/${encodeURIComponent(String(code || ""))}/`;
    function timeText(value) {
      if (!value) return "";
      const parts = String(value).split(" ");
      return parts[1] || parts[0] || "";
    }
    function renderServiceContext(data) {
      const ctx = data.service_context || {};
      const chips = [
        `${ctx.service_trade_date || data.trade_date || "最近交易日"} ${ctx.phase_label || "观察"}`,
        ctx.base_trade_date ? `底稿 ${ctx.base_trade_date} 收盘` : "",
        data.leader_data_trade_date ? `领头羊 ${data.leader_data_label || data.leader_data_trade_date}` : "",
        ctx.root_cache_updated_at ? `证据缓存 ${timeText(ctx.root_cache_updated_at)}` : "",
        ctx.post_close_status_label ? `盘后 ${ctx.post_close_status_label}` : "",
        ctx.research_pool_count ? `研究池 ${fmt(ctx.research_pool_count)}只` : ""
      ].filter(Boolean);
      $("serviceContext").innerHTML = chips.map(item => `<span>${esc(item)}</span>`).join("");
    }
    const dimOrder = { today_limit: 1, first_limit_10d: 2, limit_up_days: 3, trend_strength: 4, pool_rank: 5, first_limit: 6, "3d": 7, "5d": 8, "10d": 9 };
    const state = { scopes: [], activeKey: "", filter: "" };
    function queryTradeDate() {
      return clean(new URLSearchParams(window.location.search).get("trade_date"));
    }
    function queryDate() {
      return $("tradeDate").value || queryTradeDate();
    }
    function scoreCell(label, value) {
      const text = value === null || value === undefined || value === "" ? "-" : String(value);
      return `<div class="score-cell"><span>${esc(label)}</span><b>${esc(text)}</b></div>`;
    }
    function clip(value, limit = 72) {
      const text = String(value || "").replace(/\s+/g, " ").trim();
      return text.length > limit ? `${text.slice(0, limit)}...` : text;
    }
    function splitTags(value) {
      return String(value || "")
        .split(/[、,，\s]+/)
        .map(item => item.trim())
        .filter(Boolean)
        .slice(0, 5);
    }
    function parseJsonArray(value) {
      if (Array.isArray(value)) return value;
      if (!value || typeof value !== "string") return [];
      try {
        const parsed = JSON.parse(value);
        return Array.isArray(parsed) ? parsed : [];
      } catch {
        return [];
      }
    }
    function factLine(label, value, className = "") {
      const text = clip(value, className === "theme" ? 110 : className === "active" ? 130 : 84);
      return text ? `<div class="fact-line ${esc(className)}"><strong>${esc(label)}</strong>${esc(text)}</div>` : "";
    }
    function renderActiveFacts(row) {
      const facts = parseJsonArray(row.active_facts)
        .map(item => ({
          date: String(item?.date || "").trim(),
          title: String(item?.title || "").trim(),
          body: String(item?.body || "").replace(/\s+/g, " ").trim(),
          lines: parseJsonArray(item?.lines).map(clean).filter(Boolean)
        }))
        .filter(item => item.body || item.lines.length || item.title)
        .sort((a, b) => String(b.date || "").localeCompare(String(a.date || "")) || factPriority(a) - factPriority(b));
      if (!facts.length) return factLine("有效事实总结", row.active_fact_summary, "active");
      const seen = new Set();
      const items = facts.filter(item => {
        const key = `${item.date}|${item.title}|${item.body.slice(0, 48)}`;
        if (seen.has(key)) return false;
        seen.add(key);
        return true;
      }).slice(0, 4);
      return `<div class="fact-timeline">
        <strong>有效事实总结${Number(row.active_fact_count || 0) > items.length ? ` · ${fmt(row.active_fact_count)}条` : ""}</strong>
        ${items.map(item => {
          const lines = item.lines.length ? item.lines : [cleanFactBody(item)].filter(Boolean);
          return lines.slice(0, 3).map((line, index) => `<div class="fact-item">
            <span class="fact-date">${esc(item.date || "-")}</span>
            <span class="fact-text">${index === 0 && item.title ? `<span class="fact-title">${esc(item.title)}</span> ` : ""}${esc(clip(line, 118))}</span>
          </div>`).join("");
        }).join("")}
      </div>`;
    }
    function cleanFactBody(item) {
      const title = String(item?.title || "").trim();
      let body = String(item?.body || "").replace(/\s+/g, " ").trim();
      body = body.replace(/^\d{2}-\d{2}\s*/, "");
      if (title) {
        body = body.replace(new RegExp(`^${escapeRegExp(title)}[：:：\\s]*`), "");
      }
      return body || title;
    }
    function escapeRegExp(value) {
      return String(value || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    }
    function factPriority(item) {
      const text = `${item?.title || ""} ${item?.body || ""}`;
      if (/股权转让|控制权|并购|重组|中标|订单|合同|客户|业绩|净利|营收/.test(text)) return 0;
      if (/异动提醒|涨停原因/.test(text)) return 1;
      if (/龙虎榜|净买入|机构|游资/.test(text)) return 2;
      return 5;
    }
    function dimValue(dim) {
      const valueText = String(dim.value_text || "");
      const isLimitTime = ["first_limit", "first_limit_10d"].includes(dim.dimension)
        || (dim.dimension === "today_limit" && !valueText.startsWith("今日"));
      if (isLimitTime) {
        const amount = Number(dim.value_num || 0);
        return `${valueText || "-"}${amount > 0 ? ` / ${amount.toFixed(2)}亿` : ""}`;
      }
      return valueText || "-";
    }
    function renderLeader(row) {
      const dimensions = (Array.isArray(row.dimensions) ? row.dimensions : [])
        .sort((a, b) => (dimOrder[a.dimension] || 9) - (dimOrder[b.dimension] || 9));
      const isTrend = row.pool_type === "trend";
      const scoreLabel = isTrend ? "趋势分" : "情绪分";
      const trendDim = dimensions.find(dim => dim.dimension === "trend_strength") || {};
      const tags = splitTags(row.headline_theme_tags);
      const scoreGrid = isTrend
        ? `
          ${scoreCell("5日排名", trendDim.rank_no ? `#${trendDim.rank_no}` : (row.rank_5d ? `#${row.rank_5d}` : "-"))}
          ${scoreCell("5日涨幅", row.pct_5d == null ? "-" : pct(row.pct_5d))}
          ${scoreCell("今日涨幅", row.today_pct == null ? "-" : pct(row.today_pct))}
        `
        : `
          ${scoreCell("日内主动性", row.today_limit_score)}
          ${scoreCell("阶段先手性", row.first_limit_10d_score)}
          ${scoreCell("连板辨识度", row.limit_up_days_score)}
        `;
      const rankClass = !isTrend && Number(row.rank_no) === 1
        ? " primary"
        : !isTrend && Number(row.rank_no) === 2
        ? " rank-2"
        : !isTrend && Number(row.rank_no) === 3
        ? " rank-3"
        : "";
      return `<article class="leader${isTrend ? " trend" : ""}${rankClass}">
        <div class="rank-cell">
          <div class="rank">${esc(row.rank_no)}</div>
          <span class="type-badge${isTrend ? " trend" : ""}">${esc(row.pool_type_label || (isTrend ? "趋势票" : "情绪票"))}</span>
        </div>
        <div class="leader-identity">
          <a class="stock" href="${stockUrl(row.code)}" target="_blank" rel="noopener noreferrer">
            <strong>${esc(row.name)}</strong>
            <small>${esc(row.code)}</small>
          </a>
          ${tags.length ? `<div class="fact-tags">${tags.map(tag => `<span>${esc(tag)}</span>`).join("")}</div>` : ""}
        </div>
        <div class="leader-signal">
          <div class="score-line"><strong>${fmt(row.total_score)}</strong><span>${esc(scoreLabel)}</span></div>
          <div class="score-grid compact">
            ${scoreGrid}
          </div>
          <div class="dim-list">
            ${dimensions.length ? dimensions.map(dim => `
              <div class="dim">
                <span>${esc(dim.label)}</span>
                <b>+${fmt(dim.score)}</b>
                <small>#${esc(dim.rank_no)} ${esc(dimValue(dim))}</small>
              </div>
            `).join("") : ""}
          </div>
        </div>
        <div class="leader-facts">
          ${factLine("公司亮点", row.company_highlights, "highlight")}
          ${renderActiveFacts(row)}
          ${factLine("本概念解释", row.theme_concept_explain, "theme")}
        </div>
      </article>`;
    }
    function scopeSub(scope) {
      const isTheme = !!scope.theme_name;
      const poolCount = scope.theme_research_pool_member_count ?? scope.universe_count;
      return isTheme
        ? `主题均涨 ${pct(scope.theme_avg_rise)} / 完整成分 ${fmt(scope.theme_member_count)} / 研究池交集 ${fmt(poolCount)}只`
        : `研究池全集 ${fmt(scope.universe_count)} 只，近5日涨停 + 近5日无涨停且5日涨幅Top30`;
    }
    function topLeader(scope) {
      const leaders = Array.isArray(scope.leaders) ? scope.leaders : [];
      return leaders.find(item => item.pool_type === "emotion") || leaders[0] || {};
    }
    function visibleScopes() {
      const keyword = state.filter.trim().toLowerCase();
      if (!keyword) return state.scopes;
      return state.scopes.filter(scope => {
        const leaderNames = (Array.isArray(scope.leaders) ? scope.leaders : [])
          .map(item => `${item.name || ""} ${item.code || ""}`)
          .join(" ");
        return `${scope.scope_name || ""} ${leaderNames}`.toLowerCase().includes(keyword);
      });
    }
    function renderTabs(scopes) {
      const filtered = visibleScopes();
      $("scopeCount").textContent = state.filter ? `${fmt(filtered.length)}/${fmt(scopes.length)}组` : `${fmt(scopes.length)}组`;
      $("scopeTabs").innerHTML = filtered.length ? filtered.map(scope => {
        const leader = topLeader(scope);
        const active = scope.scope_key === state.activeKey ? " active" : "";
        const score = leader.total_score === null || leader.total_score === undefined ? "-" : fmt(leader.total_score);
        const leaderText = leader.name ? `${leader.name} ${leader.code || ""}` : "暂无";
        return `<button class="theme-tab${active}" type="button" data-scope-key="${esc(scope.scope_key)}">
          <div class="theme-tab-top">
            <span class="theme-tab-name">${esc(scope.scope_name)}</span>
            <span class="theme-tab-score">${esc(score)}</span>
          </div>
          <div class="theme-tab-sub">
            <span class="theme-tab-leader">${esc(leaderText)}</span>
            <span>${esc(fmt(scope.universe_count))}只</span>
          </div>
        </button>`;
      }).join("") : `<div class="empty">没有匹配题材</div>`;
    }
    function overviewCell(label, value, hot = false) {
      return `<div class="overview-cell${hot ? " hot" : ""}"><span>${esc(label)}</span><strong>${esc(value)}</strong></div>`;
    }
    function renderScope(scope) {
      const leaders = Array.isArray(scope.leaders) ? scope.leaders : [];
      const emotionLeaders = leaders.filter(item => item.pool_type !== "trend");
      const trendLeaders = leaders.filter(item => item.pool_type === "trend");
      const isTheme = !!scope.theme_name;
      const sub = scopeSub(scope);
      const leader = topLeader(scope);
      const topName = leader.name ? `${leader.name} ${leader.code || ""}` : "-";
      const topScore = leader.total_score === null || leader.total_score === undefined ? "-" : fmt(leader.total_score);
      const leaderPct = leader.today_pct === null || leader.today_pct === undefined ? "-" : pct(leader.today_pct);
      return `<section class="scope">
        <div class="scope-head">
          <div class="scope-title">
            <strong>${esc(scope.scope_name)}</strong>
            <small>${esc(sub)}</small>
          </div>
          <div class="scope-meta">
            <span class="pill">${isTheme ? "头条题材" : "全市场"}</span>
            <span class="pill">情绪候选 + 趋势候选</span>
          </div>
        </div>
        <div class="scope-overview">
          ${overviewCell("成分数量", `${fmt(scope.universe_count)}只`)}
          ${overviewCell("当前领头", topName)}
          ${overviewCell("领头分", topScore, true)}
          ${overviewCell("今日涨幅", leaderPct)}
        </div>
        ${leaders.length ? `<div class="leaders">${[...emotionLeaders, ...trendLeaders].map(renderLeader).join("")}</div>` : ""}
        ${leaders.length ? "" : `<div class="empty">暂无领头羊数据</div>`}
      </section>`;
    }
    function renderActiveScope() {
      const filtered = visibleScopes();
      const scope = filtered.find(item => item.scope_key === state.activeKey)
        || state.scopes.find(item => item.scope_key === state.activeKey)
        || filtered[0]
        || state.scopes[0];
      if (!scope) {
        $("scopeStage").innerHTML = `<div class="empty">暂无领头羊数据</div>`;
        return;
      }
      state.activeKey = scope.scope_key;
      renderTabs(state.scopes);
      $("scopeStage").innerHTML = renderScope(scope);
    }
    function selectScope(scopeKey) {
      state.activeKey = scopeKey;
      try { localStorage.setItem("leader_active_scope", scopeKey); } catch (error) {}
      renderActiveScope();
    }
    async function load() {
      try {
        const params = new URLSearchParams();
        if (queryDate()) params.set("trade_date", queryDate());
        const response = await fetch(`/api/leaders?${params.toString()}`, { cache: "no-store" });
        const data = await response.json();
        if (!$("tradeDate").value && data.trade_date) $("tradeDate").value = data.trade_date;
        renderServiceContext(data);
        const scopes = Array.isArray(data.scopes) ? data.scopes : [];
        state.scopes = scopes;
        let saved = "";
        try { saved = localStorage.getItem("leader_active_scope") || ""; } catch (error) {}
        state.activeKey = scopes.some(item => item.scope_key === state.activeKey)
          ? state.activeKey
          : (scopes.some(item => item.scope_key === saved) ? saved : (scopes[0]?.scope_key || ""));
        renderActiveScope();
        $("refreshText").textContent = `已刷新 ${new Date().toLocaleTimeString("zh-CN", { hour12: false })}`;
      } catch (error) {
        $("scopeStage").innerHTML = `<div class="error">${esc(error)}</div>`;
        $("refreshText").textContent = "刷新失败";
      }
    }
    $("refreshBtn").addEventListener("click", load);
    $("tradeDate").addEventListener("change", load);
    $("scopeSearch").addEventListener("input", event => {
      state.filter = event.target.value || "";
      const filtered = visibleScopes();
      if (filtered.length && !filtered.some(item => item.scope_key === state.activeKey)) {
        state.activeKey = filtered[0].scope_key;
      }
      renderActiveScope();
    });
    $("scopeTabs").addEventListener("click", event => {
      const button = event.target.closest(".theme-tab");
      if (button) selectScope(button.dataset.scopeKey || "");
    });
    load();
  </script>
</body>
</html>"""




